// Print-only routes with @page stylesheets (A-8, architecture §7). Empty until M3 U's labels.
export default function PrintLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
