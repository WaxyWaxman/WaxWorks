// O-01 Organization screen, Owner-only. Its functions ship at M1; the screen is post-v1 (§8).
export default function OrgLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
