// Store account sign-in, personal sign-in and second factor, sysadmin sign-in;
// Server Actions for the Auth admin writes (A-91). Screens land at M1 U.
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
