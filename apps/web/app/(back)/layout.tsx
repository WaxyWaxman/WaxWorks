// Back-office screens are server-rendered (A-2). Every screen under this group inherits it.
export default function BackOfficeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
