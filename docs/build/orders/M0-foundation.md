# Work order — M0 Foundation (joint)

**Flow:** none — M0 is the one order with no flow ([work-order skill](../../../.claude/skills/work-order/SKILL.md) §draft, step 1)
**Milestone / track:** M0 / joint — no track; the lane hook enforces none
**Status:** In review
**Approved by:** sr-talbot, 2026-09-20 — rows 11 and 12 struck, row 17 added at approval
**Pull request:** [#176](https://github.com/WaxyWaxman/WaxWorks/pull/176) — merged 2026-09-21 before the verdict; findings are worked in follow-up pull requests
**Register rows:** none — the register holds whole-flow scenarios and M0 delivers no flow; `/qa ready M0` is not a question this order answers

## Scope

Everything [architecture](../../architecture.md) §8 places at M0 (lines 548–549),
so that the first flow order, `E-01-D-M1` and `E-01-U-M1`, starts on a repository
that installs, typechecks, lints, runs an empty vitest suite and an empty pgTAP
suite in CI, deploys a preview, and exposes every §6 function as a typed
signature that throws `not_implemented`. This order builds **no product
behaviour**: no table, no policy, no function body, no screen. It builds the
harness those will land in, and it is the only order that writes to
`packages/contracts/` and `.github/`, because at M0 the joint skeleton *is* the
deliverable and there is no other track for it to belong to.

It is run interactively with `/develop` by a human, not by the `developer`
subagent: the subagent's hook refuses `packages/contracts/` and `.github/` and
those are rows here.

**Contract entries:** every function §6 names, as a signature and stub (A-99);
no schema and no fake behaviour.

**Reading list:** [conventions](../conventions.md) in full; `architecture.md` §1
(lines 15–33), §7 (492–541), §8 M0 rows (548–549); the §2 rows this Checklist
cites — A-1 (41), A-2 (42), A-100 (52), A-8 (150), A-7 (162), A-9 (163), A-10
(164), A-31 (165), A-85 (178), A-97 (179), A-98 (180), A-99 (181), A-101 (182),
A-102 (183), A-103 (184); §6 intro and its function table (451–486) for the
signatures; §9 rows citing A-97 to A-103. No flow. `_TBD_` conventions this
order hits: none — `conventions.md` carries none.

## Checklist

_Derived by the coordinator from the A-n decisions §8 places at M0 and the
decisions that constrain how each is built. No flow decision binds M0. One row
per decision; a decision that binds two deliverables appears once with both
named. Approved by a human before development starts; a decision missing here
is a coordinator error._

Evidence here is mostly `integration` — the deliverable is a harness, and the
test that holds it is a run of the harness itself. **A `manual` row cannot end
`Done`**: `check_coverage.py` requires a test file citing the A-n for every
`Done`, so a row nothing in the tree can hold ends `N/A` with the reason, or the
developer writes the smallest test that does hold it.

| # | Decision | Binds this order because | Evidence required |
|---|---|---|---|
| 1 | A-102 — _"pnpm workspaces, no monorepo task runner, `tsc --noEmit` per member for typecheck and ESLint with `typescript-eslint` and `eslint-config-next` for lint, driven by root `package.json` scripts; the lockfile is committed and CI installs with `--frozen-lockfile`."_ | The monorepo §8 names is this: root `package.json`, `pnpm-workspace.yaml` listing `apps/*`, `packages/*`, `e2e` and **not** `prototype/`, `pnpm-lock.yaml` committed, `pnpm typecheck` / `pnpm lint` / `pnpm test` root scripts each calling members directly | integration — `pnpm install --frozen-lockfile` then each root script exits 0 on the empty tree, output quoted; a test asserting `pnpm-workspace.yaml` excludes `prototype/` and that no Turborepo / Nx config exists |
| 2 | A-10 — _"Supabase runs locally in Docker; migrations are committed. `supabase db reset` gives a reproducible database."_ | `supabase/` initialised with `config.toml`, an empty `migrations/`, a `seed.sql` that runs to completion, and `supabase start` + `supabase db reset` succeeding from a clean checkout | integration — `supabase db reset` output quoted; a pgTAP test that the database is reachable and `select 1` returns |
| 3 | A-101 — _"A migration is `<UTC timestamp>_<verb>_<object>.sql` as `supabase migration new` mints it, ordered by filename, never edited once merged; the seed reaches the database through definer functions wherever one exists, and where it cannot, says so in a comment naming the function it stands in for."_ | The migration harness this order lays down is where the naming holds from the first file. At M0 the seed writes nothing (no table exists), so the discipline is a comment block at the head of `seed.sql` stating the rule, the two exceptions, the claims-per-block form and the refusal against an existing Organization; a lint (script or pgTAP) that every file under `supabase/migrations/` matches `^\d{14}_[a-z]+_[a-z0-9_]+\.sql$` | unit — the filename lint, with a fixture that fails it; the seed header present |
| 4 | A-103 — _"Every definer function starts from one header: `security definer`, `set search_path = ''` with every object schema-qualified, `execute` revoked from `public` and granted to `authenticated` alone (to the `sysadmin` role for an S function), owned by a dedicated non-superuser role that owns the `app` schema, A-94's `p_request_id` and `p_terminal_id` beside the actor arguments, and the first statements of the body asserting tenant, then principal and actor, then the terminal, then the M/O PIN resolution."_ | M0 writes no function body, but it creates the **`app` schema and its dedicated non-superuser owner role** in the first migration, so that M1's first function has an owner that is not `postgres`. The header text itself is [conventions](../conventions.md) §3.3, not this order's to write | integration — a pgTAP test that schema `app` exists, its owner is not a superuser, and the owner is not `postgres` |
| 5 | A-97 — _"CI runs six jobs, wired at M0, and a suite with nothing to run passes rather than being absent."_ In order: `check_docs.py`; `check_coverage.py`; typecheck; lint; vitest; pgTAP against a local Supabase started in the runner. Playwright is a seventh job and is **not** wired at M0. | `.github/workflows/build-check.yml` with the six jobs in A-97's order, pgTAP running against `supabase start` in the runner, installs with `--frozen-lockfile`. `docs-check.yml` stays as it is — deterministic and keyless — because A-97 lists its two scripts as jobs one and two and they may stay in their own file or move; either satisfies the row, the developer says which | integration — the workflow's own green run on this order's pull request, job names and durations quoted; no Playwright job present |
| 6 | A-98 — _"A contract is one file per database function exporting a Zod input schema, a Zod output schema, and one wrapper of a single shape; the fake is chosen by an explicit client at the composition root, never by an environment variable read inside a wrapper; `db-types` is regenerated by D in the same pull request as the migration that changed the schema, and CI fails on a diff."_ | `packages/contracts/` exists with `zod` as its one schema dependency, the `WaxClient` interface with its `rpc` shape, and a **separate entry point** for the fake (empty at M0 — no function has behaviour). `packages/db-types/` exists with the generated output of `supabase gen types typescript --local` against the M0 database, committed, and a CI step that regenerates and fails on a diff | unit — a test that no file under `packages/contracts/src` reads `process.env`; integration — the `db-types` diff step green in CI, quoted |
| 7 | A-99 — _"The M0 contracts skeleton is every §6 function's name, argument list and return shape, with a stub wrapper that throws `not_implemented`; the input and output schemas, the wrapper body and the fake's behaviour land in the contract pull request that opens the milestone building the function."_ | One file per §6 function, under the §6 domain the table places it in, exporting a typed signature whose argument list carries `p_actor_user_id`, `p_actor_initials`, `p_request_id`, `p_terminal_id` (A-94, A-103) and `p_manager_pin` for **M**/**O**, and a wrapper that throws `not_implemented`. The `settings_*` family is one file per configuration group §5 names. **The function list is §6's, read on the day, not this order's** — a function §6 names and the skeleton lacks is a row failure; one the skeleton has and §6 does not name is a contract change nobody approved | unit — a test enumerating the §6 function names (from a committed list the developer derives and the reviewer checks against §6) and asserting each exports a wrapper that throws `not_implemented` |
| 8 | A-2 — _"Next.js App Router on Vercel. Till screens are client components; back-office screens are server-rendered."_ | `apps/web/` scaffolded as Next.js App Router with §7's seven route groups present as empty segments — `(till)`, `(back)`, `print`, `api/cron`, `(auth)`, `(org)`, `(admin)` — and nothing in them but a placeholder page, so that M1 U adds screens to a tree that already has the shape §7 fixes | unit — a test that the seven route-group directories exist under `apps/web/app/` |
| 9 | A-9 — _"Tailwind + shadcn/ui. The prototype's design tokens map onto Tailwind theme variables."_ | Tailwind and shadcn/ui installed in `apps/web`; the prototype's design tokens mapped into the Tailwind theme once, here, so every later screen consumes them by name. The token source is `prototype/`'s stylesheet, harvested (§7) | integration — `pnpm typecheck` and `pnpm lint` green with the theme in place; a unit test that the theme file defines every token name the prototype's stylesheet defines |
| 10 | A-1 — _"Online-only in v1. No offline queue. The PWA caches the app shell; writes fail visibly rather than queuing."_ | The PWA manifest §8 names, and a service worker that caches the **app shell only** — no write is queued, no request is replayed. Nothing else about offline is built | unit — a test that the service worker's cache list contains no route under `api/` and registers no background-sync handler |
| 11 | ~~A-8 — _"No local print agent in v1. Barcode labels and the letter-size finalize summary use browser printing (`window.print()` with `@page` rules)."_ | The `app/print/` route group exists (row 8) and carries one `@page` stylesheet, empty of content, so M3 U's label stylesheet has a home. **Doubt stated:** this may be nothing more than row 8 already delivers; the human strikes it at approval if so | N/A — or unit, a test that `app/print/` carries a stylesheet with an `@page` rule, if kept~~ **Struck at approval by sr-talbot, 2026-09-20** — row 8 already creates the route group; the stylesheet lands with M3 U, the schedule with M5 D |
| 12 | ~~A-7 — _"A `jobs` table drained by Vercel Cron."_ | `app/api/cron/` exists (row 8) and `vercel.json` declares one cron schedule pointing at it, handler returning 204 with no work — the `jobs` table itself is M5's. **Doubt stated:** the schedule could equally land at M5 with the table; the human decides whether an empty drain belongs here | N/A — or integration, the cron route returning 204 on the preview deployment, if kept~~ **Struck at approval by sr-talbot, 2026-09-20** — row 8 already creates the route group; the stylesheet lands with M3 U, the schedule with M5 D |
| 13 | A-100 — _"Three environments and no others: local, preview, production. A preview deployment runs against a per-branch ephemeral Supabase branch seeded from `seed.sql`, never against production and never against a copy of it."_ | Vercel project linked with a **preview deployment per pull request**, each against a **Supabase branch** created for that pull request; the anon key and URL wired per environment; the service-role key set **only** as a server-runtime variable on preview and production and present in no file. This order's own pull request is the first preview | integration — the preview URL for this pull request, its Supabase branch name, and a `curl` of a page quoted; a unit test that no file in the repository contains `SUPABASE_SERVICE_ROLE` outside `.env.example` as a name with no value, and that no `NEXT_PUBLIC_*` name contains `SERVICE` or `SECRET` |
| 14 | A-100 — _"…the preview URL is access-protected, because an unprotected one is an unauthenticated application against a real database on the public internet."_ | Preview deployment protection enabled on the Vercel project. A setting, not code (§11 line 855); the repository cannot assert it | manual — cannot be held by a test in the tree, so the row ends **`N/A`** with the Vercel setting's name and who enabled it recorded in the Note, and the verifying human named in **Needs a human** |
| 15 | A-85 — _"The end-to-end suite is a top-level `e2e/` directory, outside both apps."_ | `e2e/` exists as a workspace member (A-102) with its own `package.json` depending on `@playwright/test`, a `playwright.config.ts` reading `PLAYWRIGHT_BASE_URL`, an empty `fixtures/`, and **no spec** — the specs are QA's, written by `/qa automate`; no Playwright job in CI (A-97) | integration — `pnpm --filter e2e exec playwright test --list` exits 0 listing zero tests, quoted |
| 16 | A-31 — _"The agent configuration is version-controlled."_ | `.claude/launch.json` gains an `apps/web` dev-server entry beside the prototype's, so `/qa walk` and `preview_start` can target the product; `.gitignore` covers `node_modules`, `.next`, `supabase/.branches`, `.vercel`, `.env*.local`, `test-results`, `playwright-report` | unit — a test that `.gitignore` contains each of those names; `launch.json` entry present |
| 17 | A-92 — _"Operational logs are the platform's, sit outside A-90's grant and so outside the product's read boundary, are read by System Administrators only through the platforms' own consoles, and are never the audit record."_ Error events go to Sentry (§1). | §8's M0 U cell names Sentry; A-92 is the decision that says what it is. At M0 the SDK is installed in `apps/web` with a **DSN per environment as a server-runtime variable** (A-100's secrets rule), initialised, and **emitting nothing** — no function exists to raise a system flag and a refusal is not an error event (§6). What an event may carry (A-93, A-94) binds the first function at M1, not this order | unit — a test that no repository file contains a Sentry DSN value (the `https://…@…ingest…` shape) and that the Sentry config reads its DSN from the environment; integration — `pnpm typecheck` green with the SDK in place |

Sentry is row 17, added at approval and citing A-92, which is the decision that
says what Sentry is. Its content rules (A-93, A-94) bind the first function that
writes an event, at M1, not this order.

## Evidence

_Written by the developer. One row per Checklist row, in the same order. Every
cell is exactly one of `Done`, `Deferred`, `Blocked`, `N/A` — a blank cell fails
`check_coverage.py`._

_Rows 11 and 12 were struck at approval and still count as Checklist rows for
the gate; each ends `N/A — struck at approval by sr-talbot, 2026-09-20`._

_**The cells below are the record of the build in [#176](https://github.com/WaxyWaxman/WaxWorks/pull/176), and are left as written.**
The fix pull request moved some of what they quote, and the Findings table is where
the current state is read. Specifically: row 2's `supabase test db` is now Files=4,
Tests=19 (findings 1, 2, 5); row 4's closing-comment consequence is now recorded in a
correcting migration (finding 3); row 6's real client calls `supabase.schema("app").rpc`
(finding 5); row 7's return shapes are no longer all `z.unknown()` and `resolve_scan`
carries `p_code` (findings 14, 15); row 16's `.gitignore` is now `.env*` with
`!.env.example` (finding 9); row 17's `instrumentation.ts` no longer wires
`onRequestError` (finding 8). The vitest count quoted across several rows was 307 at
#176 and is 330 here. No row's terminal state changed._

| # | State | Test (name and `file:line`) | Run output | Note |
|---|---|---|---|---|
| 1 | Done | `A-102 — pnpm workspaces, no monorepo task runner, tsc and ESLint per member` at `tests/workspace.test.ts:10` | `pnpm test:repo`: Test Files 1 passed (1) · Tests 4 passed (4). Root scripts against the finished tree quoted under row 5's CI run. | Deps pinned: typescript ^5.9.3 (TypeScript 7 is the native compiler and typescript-eslint 8 does not yet target it), vitest ^5, pnpm 11.0.9 via packageManager. `test` runs members recursively then the repo checks; `typecheck`/`lint` recurse with `--if-present`. |
| 2 | Done | `A-10 — the database is reachable and pgTAP runs` at `supabase/tests/0001_database_reachable.test.sql:5` | `supabase db reset`: Recreating database… / Applying migration 20260921012717_create_app_schema_and_owner.sql… / Seeding data from supabase/seed.sql… / Finished supabase db reset on branch main. exit 0. `supabase test db`: 0001_database_reachable.test.sql .. ok / Files=2, Tests=8 / Result: PASS, exit 0. | `supabase init` (config.toml, Supabase CLI 2.117.0, Postgres image pulled by `supabase start`). Started with `-x studio,imgproxy,inbucket,edge-runtime,logflare,vector,realtime,storage-api,mailpit,supavisor` — the database and API only; CI uses the same exclusion. Docker Desktop 29.8 had to be added to PATH for the CLI to find it. |
| 3 | Done | `A-101 — a migration is <UTC timestamp>_<verb>_<object>.sql, ordered by filename; the seed reaches the database through definer functions` at `tests/migrations.test.ts:10` | Red: 2 failed \\| 296 passed (298) before the seed header existed. Green: `pnpm test:repo` Test Files 8 passed (8) · Tests 298 passed (298) — filename lint passes on the one migration and refuses five bad shapes; seed header carries every A-101 rule; seed has no `insert`. | The one migration was minted by `supabase migration new` and is `20260921012717_…` (UTC — local time was 2026-09-20 evening). `seed.sql` is a header and `select 1`: no table exists at M0. The refusal and the claims-per-block form are stated in the header and land as SQL with M1's first block. |
| 4 | Done | `A-103 — schema app exists / owned by waxworks_app / owner is not postgres / not a superuser / cannot log in` at `supabase/tests/0002_app_schema_owner.test.sql:7` | `supabase test db`: 0002_app_schema_owner.test.sql .... ok / Files=2, Tests=8 / Result: PASS. **No red run of this suite as such:** the first `supabase start` failed applying the migration (`ERROR: must be able to SET ROLE "waxworks_app" (SQLSTATE 42501)`), which is the failing state the suite exists to catch; after the fix the suite ran once, green. | Migration creates `waxworks_app` (nologin, nosuperuser) if absent, grants it to `postgres` — Supabase's `postgres` is not a superuser and needs membership to hand a schema or function to the role — then `create schema app authorization waxworks_app` and `usage` to `authenticated` and `anon`. **Consequence for conventions §3.3, settled by A-104 (2026-09-21):** migrations run as `postgres`, so ownership had to be arranged; A-104 chose a `set role waxworks_app` opener over a per-function `alter … owner to` line, and conventions §3.3 now shows it. This migration's closing comment still names the rejected form; A-101 makes it permanent, and a correcting migration says so. |
| 5 | Done | `A-97 — six CI jobs` — the workflows themselves: `.github/workflows/docs-check.yml` (jobs 1–2 + the per-order gate) and `.github/workflows/build-check.yml` (jobs 3–6); held in the tree by `tests/e2e-layout.test.ts:34` (no Playwright job) and by `tests/workspace.test.ts` (A-102's scripts the jobs call) | PR #176 run 35551675934 `check` (jobs 1–2) pass 8s: "0 error(s), 10 warning(s)". Run 35551675950: `3 typecheck` pass 20s · `4 lint` pass 21s · `5 vitest` pass 22s ("Test Files 10 passed (10) · Tests 307 passed (307)") · `6 pgTAP` pass 1m48s ("Applying migration 20260921012717_create_app_schema_and_owner.sql… / Files=2, Tests=8 / Result: PASS"). No Playwright job in either run. | **The first run's per-order gate was a false green**: on the shallow clone `git diff origin/main...HEAD` had no merge base, `\\|\\| true` swallowed the fatal, and the step printed "no work order touched" and passed while rows 5 and 6 were blank. Fixed in the next commit: `fetch-depth: 0`, `set -euo pipefail`, explicit `merge-base`, no `\\|\\| true`; simulated locally it found the order and reported its two blank rows. Second run, 35552466941 `check` pass 6s: "work orders touched: docs/build/orders/M0-foundation.md / == docs/build/orders/M0-foundation.md / 17/17 checklist rows accounted for; 0 error(s)"; build-check jobs 3–6 pass again (22s · 28s · 22s · 1m57s). Jobs 1–2 stayed in `docs-check.yml` (A-96 amended its header; CONTRIBUTING names it); 3–6 are `build-check.yml`. Jobs run in parallel, named in A-97's order. |
| 6 | Done | `A-98 — a contract is one file per function: Zod schemas and one wrapper shape; the fake is chosen by construction, never by an environment variable` at `tests/contracts.test.ts:53` | Unit: `pnpm test:repo` — zod the one schema dep; no file under `packages/contracts/src` reads `process.env`; the fake is a separate `./fake` export and `index.ts` does not re-export it; every wrapper has arity 2. Integration, CI run 35551675950 job `6 pgTAP`: step "db-types is current" ran `pnpm --filter @waxworks/db-types gen` then `git diff --exit-code -- packages/db-types/index.ts` — passed (no diff). Locally: regeneration byte-identical (sha1 045f46fb4dbd before and after). | `packages/contracts`: `WaxClient` interface, `ContractError`/`NotImplementedError`, `contract()` and `stub()` builders, `header`/`managerHeader` schemas; real client in `src/supabase.ts` over `supabase-js` `rpc`; fake in `src/fake/index.ts`. An ESLint rule forbids `process.env` in the package as a second line behind the test. `packages/db-types/index.ts` generated for schemas `public,app` (both empty at M0); `gen` writes to a temp file and renames, because a failed run had truncated it to zero bytes through the redirect. |
| 7 | Done | `A-99 — the M0 contracts skeleton is every §6 function's name, argument list and return shape, with a stub wrapper that throws not_implemented` at `tests/contracts.test.ts:13` | Red: "Cannot find module ../packages/contracts/src/functions". Green: `pnpm test:repo` Test Files 7 passed (7) · Tests 294 passed (294) — 86 functions × (file exists + exported, throws `not_implemented` via the fake, header arguments present; pure helpers take §6's arguments). `pnpm typecheck` exit 0, `pnpm lint` exit 0. | `packages/contracts/src/functions.ts` is the committed list, read from §6 lines 451–486 on 2026-09-20, with domain, kind and citation per row. **Two derivations the reviewer must check against §6:** (1) the nine `settings_*` groups — §6 says *per configuration group*; I took §5's Configuration row, `store_settings`, Sections and genres, plus `settings_genre_merge`; (2) S functions carry the same header as every other — §6 says every function takes the actor arguments, and A-90 says nothing to the contrary, but a sysadmin has no `org_id`; the M1 contract PR settles it. Pure helpers and the two predicates take §6's stated arguments, not the header. Return shapes are `z.unknown()` placeholders until each milestone's contract PR (A-99). |
| 8 | Done | `A-2 — Next.js App Router on Vercel; till screens are client components, back-office server-rendered` at `tests/web-layout.test.ts:8` | Red: 9 failed \\| 5 passed (14) before the groups existed. Green: `pnpm test:repo` Test Files 2 passed (2) · Tests 14 passed (14). `pnpm typecheck` → "✓ Types generated successfully", tsc exit 0; `pnpm lint` → eslint exit 0. | Scaffolded with `create-next-app@latest --app --tailwind --eslint` (Next 16.3.5, React 19.2); its nested `pnpm-workspace.yaml`, `CLAUDE.md` and `AGENTS.md` removed. `(till)/layout.tsx` carries `"use client"`; `(back)/layout.tsx` does not — the group layout fixes A-2's rendering mode for every screen it will hold. `api/cron/route.ts` returns 404 and does no work; the drain is M5 D's (row 12 struck). `typecheck` runs `next typegen` first: Next 16 generates `LayoutProps`. |
| 9 | Done | `A-9 — Tailwind + shadcn/ui; the prototype's design tokens map onto Tailwind theme variables` at `tests/design-tokens.test.ts:12` | Red: "Cannot find module ../apps/web/design/tokens". Green: `pnpm test:repo` Test Files 3 passed (3) · Tests 19 passed (19) — 49 prototype tokens each mapped and value-equal. `pnpm typecheck` ✓, `pnpm lint` exit 0 with the theme imported. | shadcn/ui via `shadcn@latest init -d` (base-ui preset, `components.json`, `lib/utils.ts`, one Button). Tokens live in `apps/web/app/theme.css` under `@theme`, imported from `globals.css`; the rename is `apps/web/design/tokens.ts`. Every token takes a `ww-` segment because shadcn's theme already owns `--color-border`, `--color-accent`, `--font-sans`, `--font-mono` and `--radius-*`. Observation for the first U order: pointing shadcn's semantic variables at these tokens is a screen decision, not made here. |
| 10 | Done | `A-1 — online-only in v1; the PWA caches the app shell; writes fail visibly rather than queuing` at `tests/pwa.test.ts:8` | Red: 4 failed \\| 19 passed (23). Green: `pnpm test:repo` Test Files 4 passed (4) · Tests 23 passed (23). `pnpm typecheck` ✓, `pnpm lint` exit 0. | Hand-written `apps/web/public/sw.js`: precaches an `APP_SHELL` list of two static files, returns early on any non-GET, any `/api/` path and any cross-origin request; no `sync`/`periodicsync` listener, no storage. Registered from `components/register-service-worker.tsx` in the root layout. `manifest.webmanifest` standalone, start_url `/`. The scaffold's Google-font import (Geist) removed: a network fetch at build the prototype's tokens do not ask for. |
| 11 | N/A | — | — | Struck at approval by sr-talbot, 2026-09-20. Row 8 creates `app/print/`; the `@page` stylesheet is M3 U's. |
| 12 | N/A | — | — | Struck at approval by sr-talbot, 2026-09-20. Row 8 creates `app/api/cron/` returning 404 and doing no work; the schedule and the `jobs` table are M5 D's. |
| 13 | Deferred | `A-100 — the service-role key is a server-runtime variable set in the platform and present in no file` at `tests/secrets.test.ts:22` — the unit half, green: `.env.example` names the key with no value; no file assigns a service-role value, reads it from `process.env`, carries a `NEXT_PUBLIC_*SERVICE\\|SECRET*` name, or holds a service-role JWT. | Unit half: `pnpm test:repo` Test Files 9 passed (9) · Tests 303 passed (303). Integration half, **Vercel side observed**: project `pressers/wax-works` is linked and deployed this branch — preview `https://wax-works-git-build-m0-foundation-pressers.vercel.app`; `curl -I` → `HTTP 302`, `Location: https://vercel.com/sso-api?...`, `X-Robots-Tag: noindex`. **Supabase side not run**: no account, so no Supabase branch, no `NEXT_PUBLIC_SUPABASE_*` on the deployment, and the preview runs the app shell against nothing. | **Deferred — owner sr-talbot**, recorded here and in the order's Needs a human at approval: the Supabase account (on a tier with branching, A-100) does not exist yet. The Vercel project already exists and deploys a preview per pull request. What remains when the account does: a Supabase branch per pull request, `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` per environment, `SUPABASE_SERVICE_ROLE_KEY` and `SENTRY_DSN` as server-runtime variables on preview and production — **but see finding 29: no Sentry DSN may be set in any environment until A-93's scrubber ships as code (a `beforeSend` for A-93's five exclusions, `requestDataIntegration` sending no headers, and an explicit integration list). The absence of a DSN is currently the only thing keeping the tree from emitting unscrubbed events, and this row is the act that would remove it.** **A-106** (ratified 2026-09-21), and the first preview of this pull request. `next build` passes locally (exit 0, 3 routes), so the deploy has nothing to compile that has not compiled. On `accept` this becomes an open question in `architecture.md` §11 so it cannot be dropped. |
| 14 | N/A | — | — | A Vercel project setting (Deployment Protection → Vercel Authentication), not code; nothing in the tree can hold it (§11 line 855). **Enabled by sr-talbot on 2026-09-20**, recorded at approval. Re-verified at M6 with backups. **Observed from outside on 2026-09-21:** `curl -I https://wax-works-git-build-m0-foundation-pressers.vercel.app` → `HTTP 302` to `https://vercel.com/sso-api?url=…` with a `_vercel_sso_nonce` cookie — Vercel Authentication is on for this project's previews. Still `N/A` rather than `Done`: nothing in the tree can hold it, and a test that needs a live deployment is not a test the gate can run. |
| 15 | Done | `A-85 — the end-to-end suite is a top-level e2e/ directory, outside both apps` at `tests/e2e-layout.test.ts:9`; plus the suite's own listing, `pnpm --filter @waxworks/e2e run test:list` | `pnpm test:repo` Test Files 10 passed (10) · Tests 307 passed (307). Listing: `Listing tests:` / `Total: 0 tests in 0 files` / exit 0 (with `--pass-with-no-tests`; Playwright exits 1 on an empty suite otherwise). The gate first refused this row as `Done` with no test citing A-85 — the listing alone is not a test file — which is why the vitest test exists. | `e2e/` is a workspace member (A-102) with `@playwright/test` 1.63, `playwright.config.ts` reading `PLAYWRIGHT_BASE_URL` (default the prototype's 5273), `testMatch` = one spec per register row under a flow directory, empty `fixtures/` with its rule. No spec written. No browser installed and no CI job (A-97). Root `pnpm test` excludes `@waxworks/e2e`; `pnpm e2e` runs it on purpose. |
| 16 | Done | `A-31 — the agent configuration is version-controlled` at `tests/repo-hygiene.test.ts:8` | Red: 7 failed \\| 29 passed (36). Green: `pnpm test:repo` Test Files 6 passed (6) · Tests 36 passed (36). `git check-ignore -v` confirms `.next`, `next-env.d.ts`, `.env.local` are ignored. | `.gitignore` gains `.next/`, `.env*.local`, `.vercel`, `supabase/.branches`, `supabase/.temp`, `test-results`, `playwright-report`, `coverage/`; the tracked-`.claude` allowlist is unchanged. `.claude/launch.json` gains `web` → `pnpm --filter @waxworks/web run dev` on 3000. **This row wrote to `.claude/`**, which the develop rules forbid a track developer; the approved Checklist required it and M0 is joint — noted under Needs a human. |
| 17 | Done | `A-92 — error events go to Sentry; the DSN is a server-runtime variable, and at M0 nothing is emitted` at `tests/sentry.test.ts:12` | Red: 2 failed \\| 25 passed (27). Green: `pnpm test:repo` Test Files 5 passed (5) · Tests 27 passed (27) — SDK present, three configs read `process.env.*SENTRY_DSN`, no tracked file holds a DSN value, no `Sentry.capture*` call in `apps/web`. `pnpm typecheck` ✓, `pnpm lint` exit 0. | `@sentry/nextjs` 10.x; `sentry.server.config.ts`, `sentry.edge.config.ts`, `instrumentation-client.ts`, `instrumentation.ts`; `enabled` only when a DSN is set, `sendDefaultPii: false`, `tracesSampleRate: 0`. `withSentryConfig` wired with source maps disabled. `@sentry/cli`'s postinstall (binary download for source-map upload) is **denied** in `pnpm-workspace.yaml` — no auth token is decided; **Needs a human** when one is. |

## Findings

_Written by reviewers (`qa-reviewer`, `architect`, `/code-review`). The developer
marks each `fixed` or `disputed`; nothing here is deleted._

| # | Reviewer | Decision cited | `file:line` | Finding | Severity | Developer |
|---|---|---|---|---|---|---|
| 1 | architect | A-103, A-40, A-90 | `supabase/migrations/20260921012717_create_app_schema_and_owner.sql:12` | `waxworks_app` is `nosuperuser` with no `bypassrls` and no table privilege, so at M1 the first definer function is subject to every RLS policy and fails its first DML; A-103 calls the owner's privileges *the ceiling* without saying what it holds. Ratified as **A-104** on 2026-09-21. | should | **resolved by A-104** (ratified 2026-09-21): `bypassrls`, per-table DML, `set role` opener. Correcting migration in the fix PR — **fixed** — `supabase/migrations/20260921030925_grant_app_owner_ceiling.sql`:11 `alter role waxworks_app bypassrls`. Red: `supabase/tests/0003_app_owner_ceiling.test.sql` failed 2 of 8 ("Failed test 1 … Failed test 6"). Green: `supabase test db` Files=4, Tests=19, Result: PASS. The per-table DML A-104 also names lands in each table's own migration; M0 creates no table. **Correction after the re-check:** the statement is at `:33`; `:11` is comment prose. |
| 2 | architect | A-103, A-90 | `supabase/migrations/20260921012717_create_app_schema_and_owner.sql:28` | `grant usage on schema app to anon` widens a surface no decision grants; A-103 grants execute to `authenticated` alone, and without USAGE a forgotten revoke is still uncallable by `anon`. Remove; `sysadmin` will need USAGE at M1 for the S functions. | should | **resolved by A-104**: `anon` holds no usage on `app`. Correcting migration revokes it — **fixed** — `supabase/migrations/20260921030925_grant_app_owner_ceiling.sql` `revoke usage on schema app from anon`. Held by `supabase/tests/0003_app_owner_ceiling.test.sql`:36 (anon has none) and :40 (`authenticated` still does). `sysadmin` does not exist yet; the migration that creates it at M1 grants its usage there. |
| 3 | architect | A-101, A-103 | `supabase/migrations/20260921012717_create_app_schema_and_owner.sql:30-32` | The closing comment states conventions §3.3 *ends with* `alter function … owner to waxworks_app`; §3.3 ends with the revoke/grant pair and no such line exists. A-101 makes migration text permanent once merged — reword before merge, or land the amendment first. A suite-wide pgTAP assertion *every function in `app` is owned by `waxworks_app`* is wanted either way. | should | **resolved by A-104**: the opener, not a closing line; the pgTAP assertion this row asked for is A-104's. The comment itself is permanent (A-101); the correcting migration records that it described a rejected alternative — **fixed** — the first migration's text is untouched (A-101). `supabase/migrations/20260921030925_grant_app_owner_ceiling.sql` lines 8–25 record that its closing comment describes the alternative A-104 rejected, and name the `set role` opener A-104 chose instead. The suite-wide ownership assertion this row asked for is `supabase/tests/0003_app_owner_ceiling.test.sql`:49 — vacuously true at M0 with no function, and live from M1's first. **Correction after the re-check:** the record runs lines 8–19; 21–32 is the `bypassrls` rationale. |
| 4 | architect | A-94, §3 | `packages/contracts/src/wrapper.ts:10` | `p_request_id` is caller-supplied in `header`; §3 line 215 and A-94 say the wrapper mints it. Propagates to 80 files through the shared shape; retry semantics land on the screen. Fix: wrapper generates it, input schema omits it, the database signature keeps it. | should | **fixed** — `header` drops `p_request_id` (`packages/contracts/src/wrapper.ts:6`); `contract()` mints one per call at `:37` with `crypto.randomUUID()`, keyed on the header's presence so a pure helper is still given none (§6). Held by `tests/contracts.test.ts:128` — four tests: the header does not ask for one, two calls mint two different v4 uuids, a planted value is overridden, a pure helper gets none. The database signature is unchanged. |
| 5 | architect | A-103, §7 | `packages/contracts/src/supabase.ts:11`, `supabase/config.toml:13` | `supabase.rpc(fn)` resolves in `public`; A-103 creates `app.<name>`; `config.toml` exposes `public,graphql_public` only. At M1 every wrapper call 404s. Needs `supabase.schema("app").rpc(...)` and `app` exposed — which also exposes any table placed in `app`; where tables live is not specified. Ratified as **A-105** on 2026-09-21. | should | **resolved by A-105**: `app` exposed in `config.toml`, client calls `supabase.schema("app").rpc`. Fix PR — **fixed** — `supabase/config.toml` `[api].schemas = ["public", "graphql_public", "app"]`; `packages/contracts/src/supabase.ts:16` calls `supabase.schema("app").rpc`. Held by `tests/supabase-config.test.ts:30` and `tests/contracts.test.ts:180`, whose double asserts the default schema is never used and that a PGRST202 still surfaces as a typed `ContractError`. Where tables live is answered by A-105 and asserted by `supabase/tests/0004_app_holds_functions_only.test.sql`. |
| 6 | architect | A-55, A-87, §6 | `packages/contracts/src/identity/actor_resolve.ts:8` | `actor_resolve` extends `header` and so requires `p_actor_user_id` — the value it exists to produce. Same tension for `manager_authorize_pin`, `terminal_register` and the three S functions. A-99 says the signature set is M0's agreement, so this belongs here, not in M1's contract PR. Owner §6 / `/architecture`. | should | **disputed — not this order's to answer.** The tension is real and unchanged; [plan.md](../plan.md) §3 step 4 routes it to the **M1 contract pull request**, with Finding 24, as one of the three header questions. Filed under Needs a human. Encoding an answer here would be choosing a behaviour no document gives. **Correction after the re-check:** `plan.md` §3 was renumbered by this same commit — the M1 contract pull request is now **step 3**, not step 4. |
| 7 | architect | A-91, A-100 | `supabase/config.toml:175` | `enable_signup = true` is a fifth Auth-identity path outside A-91's four, built into every Supabase branch from this file. Recommend `false`; invites via the admin API still work. The production dashboard value is a human's. | should | **resolved by the A-91 amendment** (2026-09-21): `enable_signup = false`. Fix PR — **fixed** — `[auth].enable_signup = false` and `[auth.email].enable_signup = false` in `supabase/config.toml`, each commented with the A-91 amendment. Held by `tests/supabase-config.test.ts:42`, which walks every section rather than the two known ones, so a third sign-up switch in a future CLI template fails the suite. The production dashboard value stays a human's (architecture §11). Sentry's allowed-domains, raised in the same finding, is still unrecorded — Needs a human, with Finding 25. |
| 8 | architect | A-92, A-93 | `apps/web/instrumentation.ts:8`, `tests/sentry.test.ts:47` | `onRequestError = Sentry.captureRequestError` is a wired capture path with no `beforeSend` scrubber; the row-17 test greps only `capture(Exception\|Message\|Event)(`. Inert today (no DSN anywhere) but emits unscrubbed server errors the day a DSN is set unless A-93 lands first. Remove until M1, or extend the test and say so in row 17. Also: `NEXT_PUBLIC_SENTRY_DSN` is a public DSN A-92 requires; Sentry's allowed-domains is the platform control and nothing records it. | should | **fixed** — `onRequestError` removed from `apps/web/instrumentation.ts`; a comment records why and what has to land first (A-93's scrubber, M1). Held by `tests/sentry.test.ts:50`, which strips comments before matching so the record can name the hook. `NEXT_PUBLIC_SENTRY_DSN` is correct per A-92; Sentry's allowed-domains is still recorded nowhere — Needs a human, with Finding 25. — **NOT SUSTAINED on re-check, and this cell was wrong.** Independently verified: `@sentry/nextjs` 10.75.0 installs default integrations in all three `Sentry.init` calls, and **none of them is gated by `tracesSampleRate`** — browser: `globalHandlersIntegration()` (`window.onerror`, `onunhandledrejection`), `breadcrumbsIntegration()`, `browserApiErrorsIntegration()`, `linkedErrorsIntegration()`; node and edge: `requestDataIntegration()`, `consoleIntegration()`, `linkedErrorsIntegration()`, the uncaught-exception and unhandled-rejection handlers. **No `beforeSend` exists anywhere in the tree.** `@sentry/core/.../integrations/requestdata.js:113-126` copies every request header and deletes only `cookie` and the IP set — **`authorization` is never stripped**, and a session token is A-93 exclusion (2). So removing `onRequestError` closed the React-Server-Component path and no other; an uncaught `ContractError` in a till client component still reaches Sentry through the browser global handler, carrying `details` = the raw PostgREST error. **The only live control is that no DSN is set**, and row 13 schedules setting one. The removal and the tests stand as improvements; the *reasoning* recorded here — that a sample rate is what makes the rest inert — was backwards. No code change is required at M0; a recorded precondition is. See finding 29, row 13's Note, and **A-106**. |
| 9 | architect | A-100 | `.gitignore:10` | `.env*.local` only; a root `.env`, `.env.development` or `e2e/.env` is committable. Row 16 listed exactly `.env*.local`, so code matches its row. Recommend `.env*` + `!.env.example`. | observation | **fixed** — `.gitignore` is now `.env*` with `!.env.example`. Held by `tests/repo-hygiene.test.ts:30`, which asks `git check-ignore` about `.env`, `.env.local`, `.env.development`, `.env.production.local`, `e2e/.env` and `apps/web/.env` rather than reading the file's text, and `:37` asserts `.env.example` stays tracked. Row 16 named `.env*.local`; `.env*` is a superset, so the row still holds. |
| 10 | architect | A-47 | `packages/contracts/src/helpers/round_to_ending.ts:9`, `tax_rate_at.ts:9` | `_minor` as `z.bigint()` and `_ppm` as `z.number().int()`: `JSON.stringify` throws on bigint, PostgREST returns int8 as a JSON number, and A-47 forbids a JS `number` carrying money. How a bigint minor unit crosses the wire is not specified. Bites at M3's contract PR. | observation | **disputed — no decision answers it, and this order must not invent one.** Unchanged, and now symmetric: Finding 15's six return shapes mirror the input side exactly (`z.bigint()` for a minor unit, `z.number().int()` for ppm), so the fix adds no new commitment about the wire. How a bigint minor unit crosses PostgREST — which returns int8 as a JSON number, while `JSON.stringify` throws on a bigint and A-47 forbids a JS number carrying money — is unspecified. Filed under Needs a human; it bites at M3's contract pull request. **Correction after the re-check — the cell overclaimed.** "Adds no new commitment about the wire" is wrong: an **output** schema is enforced at runtime by `contract()` against what PostgREST actually returns, where an input schema only validates what a caller supplies. `tests/contracts.test.ts` now asserts a JS number is *rejected*, and PostgREST returns `int8` as exactly that, so these schemas will throw `invalid_output` on the first real call at M3. That is the louder failure and probably the right one under A-47, but it is a new commitment and it is named here rather than rediscovered. The open question is unchanged and still not this order's to answer. |
| 11 | architect | A-96 | `docs/build/orders/M0-foundation.md:90`, `.github/workflows/build-check.yml:16` | A-96 cited without its *not yet ratified* qualifier; `build-check.yml` triggers on a `staging` branch that exists only in unratified A-96. Actions pinned to mutable tags where A-96 pins to a commit and says why. `tests/secrets.test.ts:96`'s JWT fragment matches one of three base64 alignments. | observation | **disputed — the owners', not the developer's.** A-96 is still *recommended, not yet ratified*, so whether `build-check.yml` keeps its `staging` trigger and whether `security-review.yml` stays are what its ratification answers; [plan.md](../plan.md) §4 already lists it. Action pinning follows the same decision. The `tests/secrets.test.ts:96` JWT-fragment narrowing is a real point and is untouched here. Needs a human. |
| 12 | /code-review | A-94 | `packages/contracts/src/wrapper.ts:8` | Same as the architect's fourth finding: the header requires `p_request_id` from the caller while its own comment says the wrapper mints it; neither `contract()` nor `stub()` generates one. | should | **fixed** — the same change as Finding 4: `packages/contracts/src/wrapper.ts:6` and `:37`, held by `tests/contracts.test.ts:128`. |
| 13 | /code-review | A-98 | `.github/workflows/build-check.yml:77` | `supabase/setup-cli` at `latest` while `db-types` was generated with 2.117.0; a CLI release flips the regenerate-and-diff step red on every PR with nothing changed. | should | fixed — pinned to 2.117.0 with a comment saying why **Note after the re-check:** true of the tree but **not work in this pull request** — it landed in [#177](https://github.com/WaxyWaxman/WaxWorks/pull/177) and `.github/` is absent from this diff. |
| 14 | qa-reviewer | A-99 | `packages/contracts/src/catalog/resolve_scan.ts:8` | N1 — `resolve_scan`'s argument list lacks `p_code`; §6 line 469 states it as `resolve_scan(p_code)`. The only §6-stated argument missing from the 86 files. | should | **fixed** — `packages/contracts/src/catalog/resolve_scan.ts:10` adds `p_code: z.string()`, matching §6 line 470's `resolve_scan(p_code)`. Held by `tests/contracts.test.ts:45`. The **return** shape stays `z.unknown()`: §6 describes a picker payload but states no shape, and A-99 puts it in M2's contract pull request. *(The original finding cited §6 line 469; the paragraph is at line 470.)* |
| 15 | qa-reviewer | A-99 | `packages/contracts/src/helpers/*.ts` (six files) | N2 — §6 states six return shapes (`bigint`, `minor`, `ppm`, `text`, `boolean`, `boolean`) and the skeleton reduces them to `z.unknown()`. A-99 places *return shape* at M0. | should | **fixed** — the six §6-stated shapes replace `z.unknown()`: `round_to_ending` → `z.bigint()` (§6 line 461), `suggested_retail` → `z.bigint()` (line 462 — minor units, as `list_minor` already is), `tax_rate_at` → `z.number().int()` (line 463), `upc_a_check_digit` → `z.string()` (line 464), `invoice_is_paid` and `period_is_sealed` → `z.boolean()` (lines 466, 468). Each mirrors the input side rather than choosing a new representation — see Finding 10. Held by `tests/contracts.test.ts:49`, which asserts each shape both accepts its type and rejects the wrong one. |
| 16 | qa-reviewer | A-102 | `e2e/package.json:5-9`, `package.json:9-10` | N3 — *`tsc --noEmit` per member* does not hold for `e2e/`: no `typecheck` script, and `--if-present` skips it (`Scope: 4 of 5 workspace projects`). `tests/workspace.test.ts` asserts the root scripts exist, not that each member answers. | should | **fixed** — `e2e/package.json` gains `"typecheck": "tsc --noEmit"`; `e2e/tsconfig.json` already existed. `pnpm typecheck` now runs it: "e2e typecheck$ tsc --noEmit … Done". Held by `tests/workspace.test.ts:39`, which asserts **every** member answers `typecheck` and runs `tsc --noEmit`, so `--if-present` can silently skip nothing. |
| 17 | qa-reviewer | A-85 | `docs/build/orders/M0-foundation.md` row 15 | N4 — the Checklist names `pnpm --filter e2e exec playwright test --list` exits 0; it exits 1. Evidence substitutes `test:list` with `--pass-with-no-tests` and says so. Coordinator decides whether the substitution satisfies the row. | observation | **disputed — the coordinator's call, and a Checklist row is not the developer's to touch** ([workflow](../workflow.md) §1; develop §Rules). Row 15's substitution stands as the build recorded it: `pnpm --filter @waxworks/e2e run test:list` exits 0 with `--pass-with-no-tests`, and Playwright exits 1 without it on an empty suite. Nothing in this pull request changes either. Needs a human. |
| 18 | qa-reviewer | S-01 d2 | `supabase/config.toml:175`, `:220` | N5 / E3 — `enable_signup = true` in the file A-100's branches and production are provisioned from; S-01 d2 says there is no public sign-up. Same as the architect's seventh finding. | should | **resolved by the A-91 amendment** — see finding 7 — **fixed** — see Finding 7. `tests/supabase-config.test.ts:42` cites A-91; S-01 d2's *no public sign-up* is what the config now matches. |
| 19 | qa-reviewer | A-103, §3 | `supabase/config.toml:13`, `packages/contracts/src/supabase.ts:11` | E1 — `app` is not exposed to PostgREST and the real client calls `rpc` on `public`; M1's first wrapper call answers `PGRST202`. Same as the architect's fifth finding. | should | **resolved by A-105** — see finding 5 — **fixed** — see Finding 5. |
| 20 | qa-reviewer | A-92, A-93 | `apps/web/instrumentation.ts:8`, `packages/contracts/src/supabase.ts:12` | E2 — once a DSN exists, a definer function's refusal (A-92: *a designed outcome, not an error event*) reaches Sentry via `onRequestError`, carrying `ContractError.details` = the raw PostgREST error. Not live today; the wiring is. | should | **fixed** — see Finding 8. The path that would have carried `ContractError.details` to Sentry is gone until A-93 lands. — **NOT SUSTAINED on re-check, and this cell was wrong.** Independently verified: `@sentry/nextjs` 10.75.0 installs default integrations in all three `Sentry.init` calls, and **none of them is gated by `tracesSampleRate`** — browser: `globalHandlersIntegration()` (`window.onerror`, `onunhandledrejection`), `breadcrumbsIntegration()`, `browserApiErrorsIntegration()`, `linkedErrorsIntegration()`; node and edge: `requestDataIntegration()`, `consoleIntegration()`, `linkedErrorsIntegration()`, the uncaught-exception and unhandled-rejection handlers. **No `beforeSend` exists anywhere in the tree.** `@sentry/core/.../integrations/requestdata.js:113-126` copies every request header and deletes only `cookie` and the IP set — **`authorization` is never stripped**, and a session token is A-93 exclusion (2). So removing `onRequestError` closed the React-Server-Component path and no other; an uncaught `ContractError` in a till client component still reaches Sentry through the browser global handler, carrying `details` = the raw PostgREST error. **The only live control is that no DSN is set**, and row 13 schedules setting one. The removal and the tests stand as improvements; the *reasoning* recorded here — that a sample rate is what makes the rest inert — was backwards. No code change is required at M0; a recorded precondition is. See finding 29, row 13's Note, and **A-106**. |
| 21 | qa-reviewer | A-99 | `tests/contracts.test.ts:18-21` | Untested: the reverse direction — a file under `packages/contracts/src/<domain>/` that `functions.ts` does not list passes. Row 7 names this as the failure it refuses. | should | **fixed** — `tests/contracts.test.ts:67` walks file → list: every tracked file under a §6 domain directory must be a name `functions.ts` lists, with the infrastructure files and `fake/` excluded by name. **Verified failing before it was trusted:** adding `packages/contracts/src/catalog/__stray_probe.ts` turned the suite red ("Tests 1 failed \| 313 passed") and removing it turned it green again. **Clarifications after the re-check:** (a) the quoted probe run was against a 314-assertion suite, the state at that moment — "1 failed \| 313 passed (314)"; the suite is 332 now. (b) The guard read **tracked files only**, where the A-98 test twenty lines below deliberately reads tracked *and* untracked. Now fixed: it reads both, so an uncommitted contract file is caught at the moment a developer would hit it. |
| 22 | qa-reviewer | A-92 | `tests/sentry.test.ts:47` | Untested: `captureRequestError` and `captureRouterTransitionStart` are emission paths the *emitting nothing* test does not see. | should | **fixed, and the two paths are now distinguished.** `captureRequestError` is gone (Finding 8). `captureRouterTransitionStart` stays and `tests/sentry.test.ts:63` now sees it — it is navigation **tracing**, so `tracesSampleRate: 0` is what makes it emit nothing, and the test asserts that rather than the hook's absence. An error hook has no equivalent gate, which is the asymmetry that makes one removable and the other inert. — **NOT SUSTAINED on re-check, and this cell was wrong.** Independently verified: `@sentry/nextjs` 10.75.0 installs default integrations in all three `Sentry.init` calls, and **none of them is gated by `tracesSampleRate`** — browser: `globalHandlersIntegration()` (`window.onerror`, `onunhandledrejection`), `breadcrumbsIntegration()`, `browserApiErrorsIntegration()`, `linkedErrorsIntegration()`; node and edge: `requestDataIntegration()`, `consoleIntegration()`, `linkedErrorsIntegration()`, the uncaught-exception and unhandled-rejection handlers. **No `beforeSend` exists anywhere in the tree.** `@sentry/core/.../integrations/requestdata.js:113-126` copies every request header and deletes only `cookie` and the IP set — **`authorization` is never stripped**, and a session token is A-93 exclusion (2). So removing `onRequestError` closed the React-Server-Component path and no other; an uncaught `ContractError` in a till client component still reaches Sentry through the browser global handler, carrying `details` = the raw PostgREST error. **The only live control is that no DSN is set**, and row 13 schedules setting one. The removal and the tests stand as improvements; the *reasoning* recorded here — that a sample rate is what makes the rest inert — was backwards. No code change is required at M0; a recorded precondition is. See finding 29, row 13's Note, and **A-106**. |
| 23 | qa-reviewer | A-97 | `.github/workflows/build-check.yml` | Spec gap: A-97 says *runs, in order*; the four jobs have no `needs:` and run in parallel, named in order. Sequencing or listing — for `/architecture`. | observation | **disputed — owner `/architecture`, as the finding itself says.** A-97's wording (*runs, in order*) against four jobs with no `needs:` is a specification question, not a defect in the diff. Nothing here changes `build-check.yml`'s job graph. Needs a human. |
| 24 | qa-reviewer | A-99, §6 | `packages/contracts/src/functions.ts:26-27, 90-98` | Spec gaps the code chose: the nine `settings_*` groups (`store_settings` is §5's Tenancy row, not Configuration); the two predicates labelled `pure` with one argument where §6 calls them read-only and derived; S functions with the full header. Developer flagged the first and third in row 7. | observation | **disputed — routed, not answered.** [plan.md](../plan.md) §3 step 4 puts the nine `settings_*` groups, the two predicates and the S-function header in the **M1 contract pull request**, with Finding 6. Unchanged here by design. **Correction after the re-check:** `plan.md` §3 was renumbered by this same commit — the M1 contract pull request is now **step 3**, not step 4. |
| 25 | qa-reviewer | A-92 | `docs/build/orders/M0-foundation.md` row 17, `.env.example:8` | Row 17's Checklist says the DSN is *a server-runtime variable*; A-92 puts Sentry in the till's browser, which needs `NEXT_PUBLIC_SENTRY_DSN`. The code follows A-92; the row's wording does not. | observation | **disputed — the wording is row 17's, and the Checklist is not the developer's to edit** ([workflow](../workflow.md) §1). The code follows A-92 and is unchanged. Whoever amends row 17 also owns Sentry's allowed-domains, which findings 7 and 8 raise and no decision records. Needs a human. |
| 26 | qa-reviewer | — | `apps/web/package.json:17, 22` | `cn@^0.3.0` is an unrelated npm package (the helper is `lib/utils.ts`); the `shadcn` CLI ships as a runtime dependency; `@types/node ^20` vs `^24` elsewhere. `tests/**` is typechecked and linted by nothing. | observation | **partly fixed, partly disputed.** *Fixed:* `shadcn` moved to `devDependencies` — it is the CLI, a build-time tool; `@types/node` raised from `^20` to `^24.0.0`, matching every other member and the Node 24 CI pins. *Disputed, with evidence:* `cn` is **not an unrelated package**. `node_modules/cn/package.json` gives `repository: git+https://github.com/shadcn-ui/cn.git` and describes it as "Fast, small, compiled class-name merging for Tailwind CSS. Drop-in replacement for clsx + tailwind-merge" — it is shadcn's own, installed by `shadcn init` under A-9, and it is imported by `apps/web/components/ui/button.tsx:3` and `apps/web/lib/utils.ts:1`. Removing it breaks typecheck and the build. *Not done:* `tests/**` is still typechecked and linted by nothing — a real gap, larger than a manifest edit, filed under Needs a human rather than done quietly here. **Third part now fixed.** `tests/` is a workspace member (`tests/package.json`, `tests/tsconfig.json`, `tests/eslint.config.mjs`), so A-102's per-member `tsc --noEmit` and ESLint reach it. **Turning `tsc` on found three real type errors on the first run** — `contracts.test.ts` lines 27, 84 and 120, all casts from the module namespace to a `Record` that TypeScript cannot verify, all present since M0 and invisible because vitest transpiles without typechecking. Fixed with `as unknown as`. `tests/workspace.test.ts` now asserts every member answers **both** scripts, and that **no TypeScript file in the repository sits outside a member** — verified failing against a planted root file before being trusted. Requires the A-102 amendment, drafted and *not yet ratified*: A-102 still says *four workspace members*. |
| 27 | qa-reviewer | — | `docs/qa/e2e-register.md:66` | Register drift, for `/qa`: the register says `playwright.config.ts` has a `webServer` that starts the target; it does not, and A-85 does not require one. | observation | **disputed — `docs/qa/` is QA's tree and a developer never writes it** ([workflow](../workflow.md) §1). The finding is correct: `e2e/playwright.config.ts` has no `webServer` and A-85 does not require one. Routed to `/qa`; Needs a human. |
| 28 | qa-reviewer | workflow §3.6, §4 | PR #176 | **The merge preceded the verdict.** #176 merged 2026-09-21T02:06Z with the order `In review` and the Verdict blank; gates 3–5 had not run. Every fix above is a follow-up against `main`; the migration's text is now permanent under A-101. | blocks | **disputed — a process record, not a defect code can close.** #176 did merge before the verdict and nothing undoes that. What follows from it is done rather than argued: this is the follow-up pull request against `main` that [plan.md](../plan.md) §3 step 2 specifies, the first migration's text is untouched under A-101, and `supabase/migrations/20260921030925_grant_app_owner_ceiling.sql` is the correcting one. The Verdict and the merge remain a human's ([workflow](../workflow.md) §3 step 6). **Correction after the re-check:** `plan.md` §3 was renumbered by this same commit — this follow-up pull request is now **step 1**, not step 2. |
| 29 | architect | A-92, A-93 | `apps/web/instrumentation-client.ts:6`, `apps/web/sentry.server.config.ts:7`, `apps/web/sentry.edge.config.ts:4` | **Findings 8, 20 and 22 record the hazard as closed on a premise that is false.** `@sentry/nextjs` 10.75.0's default integrations are error paths, not traced paths, so `tracesSampleRate: 0` gates none of them, and no `beforeSend` exists. `requestDataIntegration` sends every header but `cookie` and the IP set, so an `Authorization: Bearer <token>` reaches Sentry — A-93 exclusion (2). The only live control is the absent DSN, and this order's row 13 schedules setting one with nothing recorded against it. | blocks | **acknowledged, not closed — verified independently and the three cells above are corrected.** No code change is required at M0 and none is made. The precondition is recorded in row 13's Evidence Note, and **A-106** (ratified 2026-09-21), which gates the DSN on an allowlist rather than on a strip of A-93's exclusions is filed under **Needs a human**. A developer cannot ratify it and does not. |
| 30 | architect | A-94 | `packages/contracts/src/wrapper.ts:18` | Minting was keyed on `"p_actor_user_id" in args` — a runtime value — where A-94's condition is *every write*. The two coincide only while every non-pure function takes that argument, which is exactly what findings 6 and 24 route to M1. An **S** function losing the argument would stop being correlated silently, and A-90 requires each to append `organizations.log`. | should | **fixed** — `isCorrelated(fn)` now reads the committed §6 list: `kind !== "pure"`. A name the list does not know mints one, so a stray argument fails loudly where a missing correlation id would not. Held by `tests/contracts.test.ts`, which asserts the mapping across **every** entry rather than a synthetic probe. The fix and its test landed together, so there is no red run to quote for this row. |
| 31 | architect | A-94 | `packages/contracts/src/wrapper.ts:36-42`, `packages/contracts/src/client.ts:11-21` | The minted id is written into the call arguments and never exposed, so A-94's *"the only bridge between an operational event and the row it concerns"* has no mechanism: no screen can attach it to a Sentry scope and no error surface can show it. **Introduced by the Finding 4 fix** — before it, the caller supplied the id and therefore held it. | should | **disputed as to this order.** The defect is real and is this pull request's doing. The remedy is not: whether the id rides on `ContractError`, on success, or both is a change to the one wrapper shape A-98 fixes, and it is settled by **A-108** (ratified 2026-09-21), which also moves the mint ahead of validation so an `invalid_input` error carries an id. Proposing is not ratifying. Filed under **Needs a human**; cheapest before 81 call sites exist. |
| 32 | architect | A-103, A-105 | `supabase/tests/` | No suite-wide assertion that `execute` is revoked from `public` on functions in `app`. A-103 rejected a bulk `revoke` as point-in-time and recorded no assertion in its place, while A-104 met the same class of forgettable line with one. A-105 raises the stakes: `app` is now a PostgREST surface, so a forgotten `revoke` on an **S** function leaves it callable by any `authenticated` principal — stopped only by A-90's in-function assertion, which A-90 calls the *second* wall. | should | **disputed — `/architecture`'s.** The gap is real and I agree with it. Writing the assertion means recording a decision A-103 deliberately did not make, which is not a developer's to take. Settled by **A-107** (ratified 2026-09-21), which splits the rule: a list-free suite-wide assertion, plus each function's own grants in the per-function smoke test. M0 creates no function, so nothing is unprotected today. |
| 33 | architect | A-99 | — | A-99's clause *"name, argument list and return shape"* is applied two ways in one set of findings: 14 and 15 were fixed on it, 6 and 24 disputed as not-this-order's on the same clause. | observation | **acknowledged — the coordinator's, against A-99.** The two halves of A-99 are in genuine tension and I do not resolve it. Note that finding 30 depends on the answer: the more of the argument list M1 may change, the more the correlation key had to stop depending on it. Filed. |
| 34 | architect | A-91, A-104, A-105 | `supabase/migrations/20260921030925_grant_app_owner_ceiling.sql:40-42` | `service_role` holds no `usage` on `app` (verified: `anon` f, `authenticated` t, `service_role` f). A-91's Auth write-back function is *"callable by the service role alone"* and under A-105 lives in `app`; neither A-103's grant rule nor A-104's usage list provides for it. **Not specified.** | observation | **disputed — M1's, not this order's.** Correct, and it bites at the migration that creates that function, not here. Filed under **Needs a human**. |
| 35 | architect | A-102 | `e2e/package.json`, `packages/db-types/package.json` | Finding 16 fixed `typecheck` per member; `lint` is still answered by two members of four, skipped silently by the identical `--if-present` mechanism. Whether A-102's *per member* reaches ESLint is a reading question — the qualifier attaches to `tsc`. | observation | **disputed — a reading of A-102, for `/architecture`.** Adding two lint scripts would be easy and would also be me deciding what A-102 means. Filed with finding 26's related gap: `tests/**`, which holds every assertion in this review, is linted and typechecked by nothing. **fixed** — `e2e` and `packages/db-types` each gain an `eslint.config.mjs` and a `lint` script, so `pnpm lint` walks five members rather than two. The reading question the cell raised is answered by the **A-102 amendment** drafted with this change (*recommended, not yet ratified*): *per member* reaches ESLint as well as `tsc`, and the guard in `tests/workspace.test.ts` holds it so the gap cannot reopen the way it did after finding 16. |
| 36 | qa-reviewer | [workflow](../workflow.md) §1 | `docs/build/orders/M0-foundation.md:59`, `tests/contracts.test.ts:39` | **The Finding 4/12 fix contradicts approved Checklist row 7.** Row 7 requires a signature *"whose argument list carries `p_actor_user_id`, `p_actor_initials`, `p_request_id`, `p_terminal_id`"*; the fix removes it and the suite now **asserts it is absent**. A-94 and §3 line 223 back the code, so the row is what is wrong — but nobody amended it, and the order would otherwise ship with a Checklist its own tests refute. | should | **disputed — and deliberately left standing.** A Checklist row is not the developer's to touch, which is the same ground findings 17 and 25 are disputed on; fixing it here would be the inconsistency this order is supposed to avoid. **The coordinator must amend row 7 at accept.** Raised into the Verdict rather than closed. |
| 37 | qa-reviewer | — | `docs/build/plan.md:47-48`, `:132` | Two merge times are labelled `Z` but are `-04:00` local, so the file appeared to show #179 merging before #176; and §4's *"until step 3 above"* is stale against the §3 the same commit renumbered. | observation | **fixed** — both merges restated as `2026-09-21 02:34Z` and `02:35Z` (true UTC, from `%aI`), and §4 now points at step 2. The mistake was `--date=format:` in `git log`, which renders local time and appended the literal `Z` I wrote. |
| 38 | qa-reviewer | [workflow](../workflow.md) §1 | `packages/contracts/src/wrapper.ts` | This pull request changes nine files under `packages/contracts/`, including the **shared header every one of the 86 functions extends**. M0's Scope does grant this order that lane, so it is permitted — but a change to `header` is a contract change in substance, and `CLAUDE.md`'s event table sends a `packages/contracts` change to both humans. | observation | **acknowledged.** Permitted by Scope, and named here rather than absorbed quietly. **Both owners should read the `header` change as a contract change**; filed for the Verdict. |
| 39 | /code-review | A-101 | `supabase/migrations/20260921030925_grant_app_owner_ceiling.sql:33` | `alter role waxworks_app bypassrls` succeeds only because the runner holds CREATEROLE **and** BYPASSRLS on PostgreSQL 16+ — a platform property nothing asserts, in a file A-101 makes permanent. On PG 15 the same statement raises `42501`. | observation | **fixed** — the migration now names the precondition in a comment, including that `[db].major_version = 17` in `config.toml` is load-bearing for this line. Verified locally: `postgres` is `rolsuper = f, rolbypassrls = t, rolcreaterole = t` on PG 17.6. |

## Verdict

**Accepted** | **Returned** — _coordinator, date_

### Needs a human

- **Row 14** — preview deployment protection was **enabled by sr-talbot on 2026-09-20**, before approval. The row ends `N/A` recording that; the repository cannot verify it, and §11 line 855 says it is re-verified at M6.
- **Rows 11 and 12** — struck at approval by sr-talbot, 2026-09-20. Row 8's route groups are the scaffolding; the stylesheet is M3 U's and the cron schedule M5 D's.
- **The `staging` branch** — A-96 (not yet ratified) puts the CI security review on pull requests into `staging`. This order opens its pull request into `main`, as every order does under the [workflow](../workflow.md). If A-96 is ratified before this order merges, whether M0 creates the branch is a question for its owners, not this order.
- **Row 13** — **there is no Supabase account yet**; sr-talbot will create it later, on a tier with branching (A-100 records it as paid). The developer builds every other row — local Supabase in Docker (row 2), pgTAP in CI (row 5) and `db-types` generation (row 6) need no account — and ends row 13 **`Deferred`, owner sr-talbot**, recorded here. On `accept` the coordinator turns that deferral into an open question in `architecture.md` §11 so it cannot be dropped; the Vercel preview for this order's pull request runs against the fake until the account exists.
- **Sentry** — row 17, added at approval.

---

_Below: everything the `/work-order check` round added, collected here rather
than left in the Findings table. Every `disputed` finding carries a question and
every one of those questions is in this list._

**Must be discharged by an owner before or at accept**

- **Amend Checklist row 7** (finding 36). It requires the argument list to carry `p_request_id`; A-94, §3 and now **A-108** put the minting in the wrapper, and `tests/contracts.test.ts` asserts the field is *absent* from the input schema. The code is right and the row is wrong, but a row is fixed at approval and only an owner amends it. Recorded in `architecture.md` §11 so it is not lost if this order closes first.
- **Confirm that A-106 closes finding 29**, the blocking one. The architect's remedy was *a recorded precondition, not a code change*; **A-106 was ratified 2026-09-21 and is on `main`**, and row 13's Note now carries it. No code changed at M0 and none was required.
- **Finding 28 cannot ever be marked `fixed`** — #176 merged before its verdict and nothing undoes that. If *every blocking finding `fixed`* is read literally this order can never be accepted, which is a defect in the phrase as applied to a process finding rather than a reason to return.

**Decisions this order produced, now ratified 2026-09-21**

- **A-106** — no Sentry DSN in any environment until the event payload is built by an allowlist. The act it gates is this order's row 13.
- **A-107** — the grant rule held in two places: a list-free suite-wide pgTAP assertion, plus each function's own grants in the per-function smoke test. From finding 32.
- **A-108** — the wrapper mints before it validates, on every call, and sends only where A-94 wants the argument; `ContractError` carries `request_id`. From finding 31. **This creates work that does not exist yet:** the wrapper still mints *after* validating, so the code and A-108 disagree until M1 lands the change and its `invalid_input` test.

**Routed to the M1 contract pull request** — both owners review it (§7, workflow §4)

- **Findings 6 and 24** — `actor_resolve`, `manager_authorize_pin`, `terminal_register` and the **S** functions cannot take the uniform header; the two predicates; the nine `settings_*` groups. A-99 puts the argument list at M0 and the schemas at the milestone, and the two halves are in tension (finding 33).
- **Finding 38** — this order changed the shared `header` every one of the 86 functions extends. M0's Scope grants it the lane, but **a change to `header` is a contract change in substance** and should be read as one.
- **Finding 34** — `service_role` holds no `usage` on `app`, and A-91's Auth write-back function is *callable by the service role alone*. Neither A-103's grant rule nor A-104's usage list provides for it. **Not specified**; it bites at the migration that creates that function.

**Routed elsewhere, with an owner**

| Item | Owner |
|---|---|
| **Finding 10** — how a bigint minor unit crosses PostgREST under A-47's prohibition on a JS `number` carrying money. Unspecified, and now enforced at runtime by the six output schemas, so it will fail loudly at M3's contract pull request rather than silently | `/architecture`, by M3 |
| **Finding 23** — A-97 says the six CI jobs *run, in order*; four have no `needs:` and run in parallel, named in order. Sequencing or listing | `/architecture` |
| **Finding 35** — A-102's *per member* is satisfied for `typecheck` and not for `lint`: `e2e` and `packages/db-types` answer no lint script and `--if-present` skips them. A reading question, not a defect | `/architecture` |
| **Finding 26, third part** — `tests/**`, eleven files and 332 assertions including every security assertion in this review, is typechecked and linted by nothing. Larger than a manifest edit | either owner |
| **Finding 27** — register drift: `docs/qa/e2e-register.md:66` says `playwright.config.ts` has a `webServer` that starts the target; it does not, and A-85 does not require one | `/qa` |
| **Finding 17** — row 15 names `playwright test --list` exiting 0; it exits 1, and the Evidence substitutes `test:list` with `--pass-with-no-tests`. Whether the substitution satisfies the row | either owner |
| **Finding 25** — row 17's wording calls the DSN *a server-runtime variable*; A-92 puts Sentry in the till's browser, which needs `NEXT_PUBLIC_SENTRY_DSN`. The code follows A-92, the row's wording does not | either owner |
| **Finding 11** — A-96 is cited without its *not yet ratified* qualifier, and `build-check.yml` triggers on a `staging` branch that exists only in unratified A-96. Actions pinned to mutable tags | sr-talbot, WaxyWaxman |
| **Sentry's allowed-domains** for the public browser DSN, and the **auth token** for source maps — raised by findings 7, 8 and 25; recorded nowhere. `@sentry/cli`'s build stays denied until the token is decided | sr-talbot |

**Cleared since this list was written** — finding 26's third part and finding 35: `tests/` is now a workspace member and `e2e` and `packages/db-types` answer `lint`, so every member answers both scripts and no TypeScript file sits outside one. **Conditional on ratifying the A-102 amendment** that ships with it, since A-102 still reads *four workspace members*.

**Recorded as a suspicion, not a finding** — whether an `authorization` header ever reaches the application's own server, given `@supabase/ssr` keeps the session in cookies and `requestDataIntegration` already drops `cookie` when `sendDefaultPii` is false. Unverified. A-106 does not rest on it.

**Independence note.** This session was both the developer on the fix pull request and the coordinator writing this Verdict. It is therefore **not** the independent check CLAUDE.md asks for — that was `qa-reviewer`, `architect` and `/code-review`, whose findings are rows 29–39 above and whose suite runs are their own, not this session's.
