# M-05 — Accounts payable

**Actor:** Manager
**Status:** Specified
**Related:** [E-02 Receive inventory](E-02-receive-inventory.md) · [E-04 Manage the inventory](E-04-manage-inventory.md) · [E-05 Point of Sale](E-05-sell-a-record.md) · [M-01 Suppliers](M-01-supplier-margin.md) · [M-07 Chart of accounts](M-07-chart-of-accounts.md)

**Job:** As a manager, I need to see what the store owes, settle supplier invoices, and get credit for stock that arrived short or damaged.

**Scope note:** accounts payable is **in scope**, formalized as [E-02](E-02-receive-inventory.md) decision 26. See the note at the foot of this document for what remains excluded.

---

## Flow

1. Manager opens accounts payable and sees suppliers carrying an outstanding balance, sorted by supplier. A lookup finds any Supplier regardless of balance, so a zero-balance one stays reachable — to review history, or to log a new entry against them.
2. Selecting a supplier shows everything outstanding with them, in one list:

| Type | Shows |
|---|---|
| **Invoices** | Invoice number, date received, linked PurchaseOrder, original amount, paid to date, balance owing |
| **Claims** | Claim number, date sent, amount, reason, status |
| **Manual entries** | Whatever the Manager typed on Create new — see below |

3. From there the Manager **ticks whatever they are settling and settles it** (decision 27) — any mix of Invoices, manual entries, Credits, Credited claims and Claim placeholders. Credits in the selection attach to the debits in the selection, money covers the shortfall, and a selection holding no debit is a **clearing** (decision 15). They can also **create a manual entry**, **void a payment already recorded**, or review **history**.

---

## Recording a payment

4. Manager selects one or more outstanding Invoices and records a payment against them:

| Field | Notes |
|---|---|
| **Method** | Cheque, credit card, EFT, cash |
| **Reference** | Free text — `Cheque 101`, `Credit card 1278`. This is what reconciles against the bank statement. |
| **Amount** | Full or partial |
| **Date** | |
| **Recorded by** | The Manager, captured automatically |

5. The Invoice's balance owing reduces accordingly. A fully-paid Invoice leaves the outstanding list for payment history — it stays readable on this screen, in a section separate from what is outstanding, naming the PaymentBatch or batches that settled it (decision 21).

**Partial payment is supported.** An Invoice carries its original amount, what has been paid, and what remains, rather than being simply open or closed.

## Applying a claim credit

6. When a supplier confirms credit for a claim ([E-04](E-04-manage-inventory.md)), the Manager marks it **Credited**. From that moment it **counts against the balance**, exactly as a manual Credit does (decision 26) — the store holds a credit memo, and how it was raised does not change what it is worth. Applying it is a separate act that decides **what it attaches to**, not whether it counts: the Manager **selects the Invoices it should settle** and that selection *is* the choice (decisions 18, 33) — inside it the credit simply fills, and the **total is not theirs to choose** (decision 23), and any excess over what is owed becomes a **remainder Credit** while the claim itself is finished (decisions 24, 25). Because it already counted, **applying it never moves the balance** — it only changes what the credit is attached to.

A claim credit settles a balance the same way a payment does — the difference is that no money leaves the store. Both are targets of the same act: **one settlement may be part claim credit and part money**, recorded as a single PaymentBatch whose targets carry which kind each was (decision 19). So "pay $68.65 with $12.40 of credit and a $56.25 cheque" is one thing the Manager does, and one artifact afterwards. This is also why claims and Invoices are shown in one list rather than two: what the store owes a supplier is the net of both.

**A manual Credit entry works the same way**, and so does a **remainder Credit** (decision 25). All three count from the moment they exist and all three attach to debits in a settlement. Attaching never double-counts, because the credit is **consumed** when it attaches: an Invoice of $68.65 against a $34.00 Credit is $34.65 owed, and settling both with $34.65 of money covers the Invoice, consumes the Credit and moves the balance by exactly the money that left. A credit only counts twice if it lingers as a standing reducer *and* is spent separately, which consuming it in the same act prevents.

---

## Manual ledger entries

7. **Create new** lets the Manager log an entry by hand — Invoice, Claim, Credit, Adjustment, or Consignment — without going through Receiving ([E-02](E-02-receive-inventory.md)) or raising a Supplier Claim ([E-04](E-04-manage-inventory.md)). It's a lump subtotal/tax/freight/misc, not itemized against any InventoryItem; the stock (or claim, or credit) behind the number is treated as inventory unlinked to items — the itemized version of this is E-02/E-04's job, and using Create new instead is a deliberate shortcut, not a replacement for either.
8. An **Invoice** entered for a Supplier carrying the **consignment** flag ([M-01](M-01-supplier-margin.md)) defaults to type **Consignment** instead — the same default a real Receiving Invoice gets.
9. A **Claim** entry is a placeholder — logged before the supplier's credit memo is in hand — and doesn't affect the balance at all until it's cleared (below). An **Adjustment** moves the balance either way, Manager's choice; a **Credit** always reduces it, the moment it's entered.

## Clearing entries

10. When a set of entries the Manager selects sums to zero — a placeholder Claim matched against the Credit that eventually replaced it, most commonly — the Manager can mark them **Cleared** against each other. This is manual reconciliation only, never automatic: nothing infers that two entries are related. Clearing changes nothing about the balance (a Claim was never counted; a Credit already was, and stays counted) — it only retires both from what still needs attention. Neither is deleted or edited beyond the clearing fields; both remain in the ledger as history.

---

## Voiding a payment

11. A PaymentBatch recorded in error can be **voided** (decision 22). The batch is never deleted or edited — the void is appended against it, the house pattern for reversing a finalized artifact ([E-02](E-02-receive-inventory.md) step 23 and d40, [E-04](E-04-manage-inventory.md)). Voiding reverses **every** target it carried: money targets return that amount to the Invoice or entry's balance owing, and credit targets return the claim to **un-applied** — whole, since decision 24 makes it binary. A settlement that emitted a **remainder Credit** (decision 25) is not undone by destroying it: the void appends a **reversing Adjustment** of equal and opposite amount (decision 30). So a void never refuses, whatever became of the remainder in the meantime, and the two rows sum to zero for a Manager to clear under decision 15.

**A batch is voided whole or not at all.** Decision 16 made the PaymentBatch the unit of the act — one cheque covering four Invoices is one thing that happened. Voiding one target out of it would leave a batch describing a settlement that never took place in that shape, which is the thing decision 16 exists to prevent. A payment that was partly right is voided and re-recorded correctly.

**An Invoice that stops being paid stops being immutable.** Immutability attaches at **paid**, not at finalize ([E-02](E-02-receive-inventory.md) d40, [architecture](../architecture.md) A-33) — so when a void takes an Invoice back out of Paid, it returns to **Finalized** and is correctable in E-02 again. This is the consequence being accepted knowingly: a document that was frozen can be unfrozen, and "immutable" therefore means *immutable while paid* rather than *immutable forever*. The alternative — an Invoice that stays locked while carrying a balance it owes — is worse, because nothing could then correct the error the void exists to correct.

---

## Gift card liability

The store's outstanding gift cards are money owed to customers, so they are reported here rather than buried in settings.

The registry lists every card with a balance: its code, current balance, issue date, last-used date, and the Customer it is associated with where there is one. The total is the store's outstanding gift-card liability.

Loading and redeeming happen at the till ([E-05](E-05-sell-a-record.md)); this is the register, not the mechanism.

---

## Requirements

- Everything in this flow is **manager-only**.
- An Invoice's balance is derived from its amount less payments and credits applied, never edited directly.
- A payment reference must be free text — the bank statement is the reconciliation surface and its formats are not the store's to control.
- A claim may only be applied against an Invoice from the same supplier. **Which** of that supplier's Invoices is the Manager's choice, made by selecting them (decisions 18, 33); neither the per-Invoice amount nor the **total** is (decisions 33, 23).
- A **Credited** claim counts against the balance immediately, **at the amount its credit memo grants** ([E-04](E-04-manage-inventory.md) d20) rather than at the amount claimed; a **Pending** claim and a **Claim placeholder** do not count at all (decisions 26, 14). The distinction is whether the supplier has agreed, not how the credit was raised.
- Any unsettled row may be ticked into a settlement (decision 27). Credits attach to debits in the same selection; what cannot attach stays as it was; a Claim placeholder contributes **nothing to the money**, ever.
- **Every** credit is consumed whole, not only a claim (decisions 24, 28). No credit ever carries a running remainder; the unattached part becomes a new remainder Credit (decision 25).
- An excess over what the supplier is owed is emitted as a **remainder Credit** — a distinct artifact, not a manual ledger entry (decision 25). It reduces the balance immediately and is never allocated again.
- A supplier's balance **may be negative**. Nothing clamps it at zero, and it is not edited to absorb an excess (decision 8).
- A settlement may mix claim credit and money; it is one PaymentBatch either way (decision 19). A payment reference is required only where money actually moved — a credit-only settlement has none to give.
- A PaymentBatch is **voided, never edited and never deleted** (decision 22). The void is appended against the original, which stays in the ledger. Voiding is all of a batch's targets or none of them.
- An Invoice's **Paid** status is therefore not permanent. Immutability holds while it is paid and releases if that payment is voided ([E-02](E-02-receive-inventory.md) d40, [architecture](../architecture.md) A-33).
- An Invoice's **due date** is derived from its payment terms and its invoice date ([E-02](E-02-receive-inventory.md) d45), never entered here and never edited here.
- Supplier currency carries through: an Invoice in a foreign currency shows its own currency and the store-currency equivalent at the configured rate ([M-06](M-06-settings.md)). Where that rate does not exist, figures in different currencies are reported separately rather than summed.
- A manual ledger entry is never itemized against an InventoryItem — that's what Receiving/Supplier Claims are for.
- Clearing entries against each other requires their signed amounts to sum to zero; it's a Manager's manual call, never inferred automatically.

---

## Inherited from other flows

**From [E-02](E-02-receive-inventory.md):**

- Invoice records — number, date, linked PurchaseOrder, and amount — originate at receiving. Accounts payable consumes them and never creates one.
- Invoices are keyed by `(supplier, invoice_number)`, not globally (decision 1).
- A finalized Invoice is **not yet locked**: costs can still be corrected and lines added directly back in E-02 until it **is paid** here, which is what makes it immutable. **Nothing in this flow marks an Invoice paid** — there is no such action, and never was. It becomes paid because settlement brought its balance to zero, and stops being paid if that settlement is voided ([architecture](../architecture.md) A-33b, A-33a; decision 22). Voiding a paid Invoice is a manager-only amendment handled in [E-04](E-04-manage-inventory.md), appended as a separate artifact against the original record.
- An Invoice carries its own **payment method** as well as its terms, both defaulted from the Supplier and overridable at receiving ([E-02](E-02-receive-inventory.md) d45, d47; [M-01](M-01-supplier-margin.md) d20). The method is an **expectation**; decision 34 pre-fills a settlement from it, and what the PaymentBatch records is what actually happened.
- An Invoice carries its own **payment terms** and a **due date derived as invoice date + terms** ([E-02](E-02-receive-inventory.md) d45). Accounts payable consumes both and edits neither: the due date is what makes a balance *overdue* rather than merely outstanding, and it is the only basis on which this flow may age anything.

**From [M-01](M-01-supplier-margin.md):**

- **An Employee may *view* a Supplier's outstanding balance and trade totals on the Supplier card** ([M-01](M-01-supplier-margin.md) d16). This does not open any part of this flow to them: recording a payment, applying a claim credit, creating a manual entry and clearing entries against each other all remain manager-only, and decision 2 below is unchanged. The distinction is the [lexicon](../lexicon.md)'s — **manager-only** gates an action an Employee cannot *perform*, which was never a statement about what they may see.
- The Supplier card links through to this screen for the rows behind the figure, rather than restating them ([M-01](M-01-supplier-margin.md) d18). Decision 3's combined list stays the single place those rows are read and worked.
- A Supplier carries a **billing address** — where payment is remitted — captured on its card ([M-01](M-01-supplier-margin.md) d13). This flow now consumes it: it is displayed as the remit-to address alongside the payment being recorded (decision 17). Display only — M-01 remains the only place it is edited.
- A Supplier carries **payment terms** ([M-01](M-01-supplier-margin.md) d19), which default onto each Invoice received from them. This flow reads the Invoice's terms, not the Supplier's — the two can differ, and the Invoice wins ([E-02](E-02-receive-inventory.md) d45).

**From [E-04](E-04-manage-inventory.md):**

- Claims arrive here already raised, batched, sent, and carrying a **Pending** or **Credited** status.
- **A claim line names an Invoice as evidence, and that is not where its credit lands** ([E-04](E-04-manage-inventory.md) d28). The line says which received shipment is being argued about; what the credit attaches to is decided here, by ticking, under decision 27. A Credited claim whose line names Invoice 55021 may perfectly well settle against Invoice 54880 or against nothing in particular — reading the reference as routing would put two mechanisms in charge of one outcome.
- **A claim a supplier declined or never answered is Abandoned in E-04 and never appears here** ([E-04](E-04-manage-inventory.md) d24). It is neither Pending nor Credited, so it surfaces under neither step 6 nor decision 12, and it never contributed to a balance in the first place (decision 26). Abandoning is a disposal, not a correction — there is nothing on this side to reverse. **A voided claim is likewise invisible here** ([E-04](E-04-manage-inventory.md) d27), and means something different: abandoned is *no money is coming*, voided is *this claim was wrong*.
- **A Credited claim counts at the amount its credit memo grants, not at the amount claimed** ([E-04](E-04-manage-inventory.md) d20). The two commonly differ — a supplier deducts the cost of the return — and the memo governs. This is the figure decision 26 puts into the balance, decision 27 attaches to debits, and decision 28 consumes whole. The claim total stays visible as what was asked for; the shortfall is never posted as a cost here.

**From [M-07](M-07-chart-of-accounts.md):**

- **A settlement names the bank account it drew on, defaulted from the Method and overridable** ([architecture](../architecture.md) A-65). A bank account is not an entity — it is an account in [M-07](M-07-chart-of-accounts.md)'s chart carrying a bank role, and **this system holds no balance for it**; decision 5 is unchanged and the bank statement stays the reconciliation surface. The field is needed because **Method alone is too coarse**: a shop paying some suppliers from one chequing account and others from a second, both by cheque, is not distinguishable by `Cheque`. Shape follows decision 34's Method exactly — pre-filled, and the Manager may change it.
- **A PaymentBatch writes its own journal entry when it is recorded** ([M-07](M-07-chart-of-accounts.md) d12), and a **void posts forward** as a reversing entry dated when the void happened rather than rewriting the original ([M-07](M-07-chart-of-accounts.md) d8) — which is decision 22's own shape, and [architecture](../architecture.md) A-33a's *reverse as recorded*, applied to the ledger.

---

## Resolved decisions

| # | Decision |
|---|---|
| 1 | Accounts payable is **in scope** (E-02 decision 26). The only exclusion is integration with a third-party payment processing system such as Square or Stripe |
| 2 | The flow is manager-only in its entirety |
| 3 | Outstanding Invoices and Pending claims are shown in **one combined list** per supplier, because what is owed is the net of both |
| 4 | **Partial payment is supported**; an Invoice tracks original amount, paid to date, and balance owing |
| 5 | A payment records method, free-text reference, amount, date, and the Manager who recorded it |
| 6 | A claim credit settles a balance the same way a payment does, without money moving |
| 7 | A claim may only be applied against an Invoice from the same supplier |
| 8 | Invoice balances are derived from payments and credits, never edited directly |
| 9 | Accounts payable **consumes** Invoice records from E-02 and never creates them |
| 10 | Outstanding **gift-card liability** is registered here, being money owed to customers |
| 11 | ~~Clarifies decisions 6–7: a Credited claim's amount isn't earmarked to one Invoice the Manager picks. Applying it distributes automatically across the supplier's outstanding Invoices, oldest received first, absorbing any remainder on the last one if the credit exceeds what's currently owed~~ — **superseded by 18**: the Manager picks the Invoices and the amounts; the automatic oldest-first distribution is retired entirely, and decision 7's plain reading is restored |
| 12 | **Manual ledger entries** (Create new) are supported for type Invoice, Claim, Credit, Adjustment, or Consignment — a lump subtotal/tax/freight/misc, not sourced from Receiving or Supplier Claims and not itemized against any InventoryItem |
| 13 | An Invoice entry (manual or real, via Receiving) for a Supplier carrying the **consignment** flag ([M-01](M-01-supplier-margin.md) decision) defaults to type Consignment instead |
| 14 | A Claim entry is a placeholder that does not affect the balance until **Cleared** against a matching Credit; an Adjustment moves the balance either way (the Manager's choice at entry); a Credit always reduces it immediately |
| 15 | **Clearing** is manual reconciliation only, never automatic: a Manager selects a set of same-Supplier entries whose signed amounts sum to zero and marks them Cleared against each other. Nothing is deleted or edited beyond the clearing fields — both stay in the ledger as history — and clearing changes nothing about the balance itself |
| 16 | Resolves the "Payment batches" open question: a Record Payment action produces **one PaymentBatch** covering whatever mix of Invoices and manual entries it paid, browsable as one row per batch and opened to see its targets — not one record per target that merely shares a reference |
| 17 | The Supplier's **billing address is displayed here as the remit-to address**, alongside the payment being recorded. **Extends the [M-01](M-01-supplier-margin.md) d13 line inherited above**, which captured the address and recorded that nothing here consumed it. Recording a payment is the moment the store asks where the cheque goes. Display only: nothing is printed, addressed or sent, and M-01 stays the only place it is edited. *Accepted consequence:* a stale address now looks authoritative at the moment it is most likely to be acted on, and nothing on this screen can tell that it is stale |
| 18 | **The Manager chooses which outstanding Invoices a Credited claim is applied to, and how much lands on each.** **Supersedes decision 11** in full — the automatic oldest-received-first distribution is retired, not kept as a default. Decision 11 was itself a clarification of 6–7, so reversing it restores decision 7's plain reading: a claim is applied *against Invoices* from the same supplier, with the Manager saying which. The change is one of policy rather than principle — [architecture](../architecture.md) §5.1's rule that balances are **derived from movements and never edited** is untouched, and an applied credit is a movement like any other. *It is not free of structure, though:* §5.1 currently derives a balance from "`ap_payments` plus credited SupplierClaims", which carries no per-Invoice credit amount at all — so recording which Invoice a credit landed on, and how much, is a schema change §5 has yet to absorb. See the open question below. *Accepted consequence:* every application is now a decision, including the trivial one where a single Invoice is outstanding and there is no judgement to exercise. No suggested split is pre-filled, because a pre-filled split is decision 11 under another name and one nobody would check |
| 19 | **A claim credit and a payment may settle the same Invoices in one action, producing one PaymentBatch whose targets record which kind each was.** **Extends decisions 6 and 16** and contradicts neither: 6 already made a credit settle a balance the same way a payment does, and 16 already made one batch cover whatever mix it paid. That a credit could be one of the things in that mix was never addressed either way — the two being separate acts was an omission, not a position. *Accepted consequence:* a batch may now have a zero money half, so the payment reference required by decision 5 becomes conditional — required where money moved, impossible where none did |
| 20 | ~~**A Credited claim may be applied in part, across several settlements, until it is exhausted.** Resolves the "Partial claim credit" open question, and is forced by decision 18 rather than chosen independently. A claim therefore carries an amount applied and an amount remaining, and leaves the outstanding list when remaining reaches zero.~~ — **superseded by 24**: a claim is applied **whole**, and an excess over what is owed is emitted as a separate **remainder Credit** rather than left inside the claim. Decision 23 removes the case that forced this row: once the Manager chooses the distribution but not the total, an under-application by choice cannot happen |
| 21 | **A settled Invoice stays readable here, in a section separate from the outstanding list, naming the PaymentBatch or batches that settled it.** **Extends step 5 and decision 16.** Step 5 says a fully-paid Invoice leaves the outstanding list *for payment history* — naming where it goes, not that it stops being visible. Decision 16 made history browsable in one direction only, a batch opened to see its targets; the reverse, an Invoice saying what paid it, was unaddressed and is the direction someone reconciling a statement actually reads. *Accepted consequence:* this section grows without bound, so it is collapsed by default and carries a count rather than rows |
| 22 | **A PaymentBatch can be voided, whole, by a Manager — never edited and never deleted.** The void is appended against the original batch, which stays in the ledger as the record of what was recorded at the time ([E-02](E-02-receive-inventory.md) **step** 23 and d40 — not E-02 d23, which is about backorders; the [lexicon](../lexicon.md) reserves *delete* for drafts and *void* for finalized artifacts). It reverses every target the batch carried: money returns to the Invoice or entry's balance owing, and a claim returns to **un-applied**, whole — *this clause originally read "returns to that claim's remaining", which decision 24 corrected by making a claim binary; the rule this row states is unchanged.* A settlement that emitted a remainder Credit reverses that too, and ~~refuses where the remainder has already been absorbed or cleared (decision 25)~~ — **superseded by decision 30**: the void appends a reversing Adjustment instead and never refuses. **Whole or not at all**, because decision 16 made the batch the unit of the act: voiding one target would leave a batch describing a settlement that never happened in that shape. A partly-wrong payment is voided and re-recorded. *Accepted consequence, and the significant one:* an Invoice taken back out of **Paid** returns to **Finalized** and becomes correctable again, so immutability ([E-02](E-02-receive-inventory.md) d40, [architecture](../architecture.md) A-33) means *immutable while paid*, not *immutable forever*. Leaving it frozen while it still owed a balance would be worse — nothing could then correct the error the void exists to correct. Recorded as [architecture](../architecture.md) **A-33a**, which completes A-33 and amends §5.1 and [PRD](../PRD.md) §5 with it |
| 23 | **The Manager chooses the distribution of a claim credit, never its total.** **Amends decision 18**, which left both to the Manager. A credit is applied **in full**, or to the limit of what the supplier is owed, whichever is smaller — the Manager decides *where* it lands across the outstanding Invoices, not *how much of it is being spent*. Decision 18's reason survives intact: the objection was to the system silently deciding which Invoices a credit hit, and it still does not. But allowing a *total* smaller than the credit made under-application a routine everyday outcome rather than a rare edge, and everything that had to be built to support it — a running remainder on the claim, and an invariant bounding the sum of applications — existed only to carry that case. *Accepted consequence:* a Manager who wants to hold part of a credit back for a future invoice cannot; the credit goes out against what is owed now, and any true excess comes back as its own artifact (decision 24) |
| 24 | **A Credited claim is applied whole or not at all. Where the credit exceeds everything the supplier is owed, the excess is emitted as a separate remainder Credit against that supplier.** **Supersedes decision 20**, and corrects the one clause of decision 22 that leaned on it. The claim's status is binary — Credited, then applied — and it carries no running remainder. What is left over stops being a *state inside the claim* and becomes a **row in the ledger**, which is the same append-a-counter-artifact move as [E-02](E-02-receive-inventory.md) step 23, [E-04](E-04-manage-inventory.md)'s amendments and decision 22's void. Two things fall out that are worth more than the tidiness. **It gives "the supplier owes us" an artifact for the first time** — the hole behind three of this flow's open questions (a leftover credit, an overpayment, and a correction that shrinks an Invoice below what was paid), each of which ended in some version of *a credit in the store's favour that nothing models*. And **it changes the shape of a money invariant that had no enforcement point**: a claim carrying a running remainder needs `sum(applications) ≤ claim amount` held under a row lock in both the apply and the void paths, or two concurrent settlements each spend the last of it. Applying whole retires the *arithmetic* — there is no sum to bound — but **not the lock**: what replaces it is an exclusivity check, that a credit attaches in at most one live settlement, and that still needs the row held under `FOR UPDATE` in both paths. *This row first read “a claim that is applied whole needs a status check”, which understated it — a status check without a lock is precisely how two settlements each spend the same credit at full value, which is worse than over-spending a remainder. Corrected here rather than in a later row, because nobody should read the old sentence and skip the lock* ([architecture](../architecture.md) §11). *Accepted consequence:* voiding a settlement that emitted a remainder must reverse the remainder too, and must **refuse** where that remainder has already been absorbed or cleared — the shape [E-06](E-06-process-a-return.md) d10 uses when a reversal's downstream effect cannot be taken back |
| 25 | **A remainder Credit is its own kind of artifact, not a manual ledger entry, and a supplier balance may go negative because of one.** It is **not** covered by decision 12, whose defining characteristic is that a manual entry is "not sourced from Receiving or Supplier Claims" — a remainder is sourced from a Supplier Claim by construction, and filing it as a Create-new Credit would quietly retire the distinction decision 12 exists to draw. It behaves as a Credit does under decision 14: it reduces the balance the moment it is emitted, and ~~it is therefore **not separately allocatable in a later settlement**~~ — **that clause is superseded by decision 27**: a remainder attaches to debits like any other credit, and the double-count is prevented by the credit being **consumed** when it attaches, not by refusing to allocate it. It needs no spending: subsequent Invoices absorb it arithmetically, and a Manager retires it from attention with decision 15's **clearing** when they judge it settled. *Accepted consequence:* a Supplier's balance can now read **below zero**, which nothing in this flow, [M-01](M-01-supplier-margin.md), or the [architecture](../architecture.md) previously contemplated. That is the honest figure — they do owe us — but every surface reporting a balance has to render a negative one rather than clamping it, and decision 8 still forbids editing a balance to absorb anything |
| 26 | **A Credited claim counts against the balance from the moment it is Credited, exactly as a manual Credit does.** **Clarifies decisions 6 and 14 and corrects the reading this flow had been carrying**, under which a Credited claim sat outside the balance until a Manager applied it while a manual Credit reduced it on entry — the same thing, a credit memo the store holds, behaving two different ways depending on which door it came in by. The line is not *claim versus manual entry*; it is **agreed versus not yet agreed**. A **Pending** claim and a **Claim placeholder** are requests the supplier has not accepted and stay outside the balance (decision 14, unchanged). A **Credited** claim has been accepted and is money in the store's favour, so it counts — which is also what decision 3 means by "what is owed is the net of both". *Accepted consequence:* a Supplier's balance can read negative purely because a large claim was accepted, before anyone has done anything about it (decision 25). That is the point: you find out you are in credit by looking, rather than by attempting a settlement and being surprised |
| 27 | **The selection is the unit of settlement, and any unsettled row may be in it.** A Manager ticks whatever they are settling — Invoices, Consignments, manual entries, Credits, Credited claims, Claim placeholders — and one rule governs the outcome: **credits in the selection attach to the debits in the selection; whatever cannot attach stays as it was; Claim placeholders retire contributing nothing; money covers the shortfall.** **Extends decision 15 rather than replacing it.** Clearing is now the case where the selection holds no debit: there is nothing for the Credit to attach to, so it **stays counted** exactly as decision 15 requires, the placeholder retires, and no money moves. The balance does not shift, nothing is deleted, and both rows remain as history. This retires an invented restriction, not a decision: the screen had been refusing any selection that was neither payable-only nor entries-summing-to-zero, which no row ever asked for and which decision 16's "whatever mix of Invoices and manual entries it paid" already contradicted. *Accepted consequence:* a Claim placeholder can be retired by including it in a settlement, which is a Manager deciding to stop chasing it. It costs nothing and moves no money, but it is a real disposal and the screen should say so rather than letting it happen quietly |
| 28 | **Every credit consumed by a settlement is consumed whole; whatever cannot attach comes back as a remainder Credit.** **Generalises decision 24** from Credited claims to *every* credit — manual Credits and remainder Credits included. The reason is the one decision 24 already gave: a credit consumed in part carries a running remainder, and bounding a running remainder needs a lock on both the consuming and the reversing path. One credit in the ledger is either attached to debits or it is not, and the part that could not attach is a **new row** rather than a diminished old one. *Accepted consequence:* settling a $20.00 Invoice with a $34.00 Credit ticked produces a $14.00 remainder Credit rather than leaving the original at $14.00, so the ledger grows a row where it used to mutate one. That is the trade the house pattern makes everywhere — append rather than edit — and it is what keeps a void able to put things back exactly |
| 29 | **A Claim placeholder carries two figures: a face amount, and a balance contribution of zero.** Decision 14 keeps a placeholder out of the balance; decision 15 clears a set whose **signed amounts sum to zero**; decision 27 says a placeholder contributes nothing to the money. On one figure those cannot all hold — a Credit plus a placeholder sums to the Credit, not to zero, and decision 15 would refuse the very pairing its own worked example was written for. So the placeholder's **face amount** is what decision 15's test sums, and its **contribution** is what the balance sees, and the two are different numbers on the same row. *This was already true in the prototype and had never been written down* — `payableEntrySignedAmount` returns the face value while `payableEntryContribution` returns zero, and the clearing test sums the former. *Accepted consequence:* one row carries two amounts that a reader will assume are the same, so anything reporting a placeholder has to say which of the two it is showing |
| 30 | **A void reverses by appending, never by deleting. Where a settlement emitted a remainder Credit, the void appends a **reversing Adjustment** of equal and opposite amount — and the void therefore never refuses.** **Supersedes decision 22's refusal clause** (“refuses where the remainder has already been absorbed or cleared”) and **resolves the remainder-chain open question**. Decision 22 already said a PaymentBatch is never deleted and never edited; the remainder it emitted was the one artifact still being **destroyed** to undo a settlement, which is a deletion the house pattern permits nowhere else — the [lexicon](../lexicon.md) reserves *delete* for drafts. Reversing it is what is done with an entry that cannot be unwound: post the countervailing one. Two things follow. **The void stops depending on what happened afterwards.** It no longer matters whether the remainder was consumed by a later settlement or cleared, because a reversing Adjustment restores the Supplier's balance either way — so void legality is no longer order-dependent along a chain of batches, and a void can always proceed. **And the pair sums to zero**, so a Manager who wants them off the list marks them Cleared against each other under decision 15, which is exactly what that decision is for. The reversal is **system-sourced, not a manual ledger entry**: decision 12's defining characteristic is that a manual entry is not sourced from Receiving or Supplier Claims, and a reversal is sourced from a void by construction — the same carve-out decision 25 makes for the remainder itself. *Accepted consequence:* voiding a settlement that emitted a remainder leaves **two** dead rows behind rather than none, and they stay until someone clears them. That is the price of never deleting, and the ledger already pays it everywhere else |
| 31 | ~~**The Manager places a claim credit only where the distribution is genuinely ambiguous — two or more debits, and less credit than they come to.**~~ — **superseded by 33**, which removes the placement entirely. This row existed to decide *when* to ask for a per-Invoice amount; decision 33 establishes that the amount was never the question. Kept because the reasoning is instructive: a rule that patches a misreading looks exactly like a rule that refines a design |
| 32 | **A reversing Adjustment is a counterweight, not a debit: it can be cleared, never settled.** **Clarifies decision 30**, which already said the remainder and its reversal “sum to zero, so a Manager marks them Cleared against each other under decision 15” — but did not say what the reversal *is* the rest of the time. It is not a bill: nobody writes a cheque against a reversal, and offering to pay one is an invitation to do something meaningless. It counts toward the balance (that is its whole job) and it is retired only by clearing. *Accepted consequence:* a reversal can only leave the ledger paired with the remainder it cancels, so a Manager who clears the remainder against something else strands it — and decision 15's sum-to-zero test is the only thing standing between that and a balance nobody can explain |
| 33 | **The choice decision 18 gives the Manager is *which* Invoices — made by selecting them — and not a per-Invoice amount.** **Clarifies decision 18 and supersedes decision 31.** Decision 18 reads “chooses which outstanding Invoices a Credited claim is applied to, **and how much lands on each**”, and that second clause was taken literally: the screen grew an amount box per debit and refused to settle until they added up. That is more than the decision was for. The Manager's control is the **selection** — to put a credit against one Invoice, tick one Invoice — and inside the ticked set the credit simply fills, in the order the rows are listed. **This is not decision 11 returning.** Decision 11 chose *which* Invoices itself, with no Manager input at all, and that stays retired; filling a set somebody chose is arithmetic. Decision 23 is untouched: the total is still not theirs. *Accepted consequence:* a credit can no longer be split unevenly across two ticked Invoices — tick both and the fill decides which ends up settled and which part-paid. The remedy is to tick one. That is a narrower control than decision 18's wording implied, and a simpler one than the screen built from it |
| 34 | **A settlement's method pre-fills from the Invoices it covers, and the Manager may change it.** Consumes [E-02](E-02-receive-inventory.md) d47. Where the ticked Invoices agree on a method it is offered; where they disagree, or where nothing carries one, the field opens empty rather than picking a winner — guessing which of two suppliers' habits should govern a mixed cheque run is exactly the kind of quiet inference decision 18 retired elsewhere in this flow. **What is recorded on the PaymentBatch is what actually happened**, never the expectation it started from: decision 5 makes the reference the thing that reconciles against a bank statement, and a method that says `EFT` because an Invoice said so while a cheque was written is worse than an empty field. *Accepted consequence:* a pre-filled method is a default nobody is required to look at, so a mixed run settled without thinking records the first Invoice's habit for all of it |
| 35 | **A Supplier on `Prepaid` terms may carry an outstanding balance, and what it means is a prepayment nobody has recorded yet — never an invoice the store still owes.** Resolves the **Prepaid and COD** open question. Prepaid money leaves **at ordering**, weeks before the Invoice exists, and [M-02](M-02-reorder-inventory.md) records nothing about money — so the first artifact this system ever sees is the Invoice itself. The balance is therefore an accurate statement about the **ledger** and a false one about the **world**: it counts, on the same footing as any other unsettled debit (decision 8), because no settlement has been recorded against it. It **ages nowhere**, since `Prepaid` produces no due date ([E-02](E-02-receive-inventory.md) d45) — outstanding without ever being overdue. The row says *prepaid — confirm it* rather than implying a bill is due. **Finalizing an Invoice never settles it**, on Prepaid terms or any other. Accounts payable instead offers a settlement **pre-filled from the Invoice** — its amount, its method (decision 34), its date — which a Manager confirms and attaches a reference to. Three reasons, and the first is decisive. **Immutability attaches at paid, not at finalize** ([E-02](E-02-receive-inventory.md) d40, [architecture](../architecture.md) A-33, A-41): auto-settling would freeze the document the instant it was created and close the correction window that decision 22's void exists to reopen. **Decision 5's reference is the reconciliation surface**, and no automatic act can supply one. And **the confirmation is the only record the system will ever hold that the prepayment happened** — it is a person asserting it, not the system inferring it. *Accepted consequence, and it is the significant one:* the system takes **amount received = amount paid** as given. It holds no record of what was actually prepaid, so a short shipment — $200.00 sent at ordering, $187.50 of goods invoiced — settles cleanly at $187.50 and the **$12.50 the supplier owes the store is invisible by construction**. Nothing detects it. A person notices it on a card statement and logs it through decision 12's *Create new* as a Credit, which is where it belongs; the screen must not imply it is checking. *Second accepted consequence:* the headline owed figure **overstates** what the store owes by the total of unconfirmed prepaid Invoices, until they are confirmed. That is the cost of counting them, and counting them is what stops them being forgotten. **`COD` falls under the same rule** — it also produces no due date — but the store does not currently ship COD, so the rule is untested there and no screen affordance is built for it |

---

## What "in scope" means here

Payment recording and accounts payable are in scope, per [E-02](E-02-receive-inventory.md) decision 26. Decision 25, which had put them out, stands unrewritten in E-02's table as the record of what was decided at the time — the repository convention is to append rather than renumber.

The exclusion that remains is **integration with a third-party payment processing system** such as Square or Stripe. Wax Works never captures card data, never authorizes or settles a card transaction, and never moves money.

The distinction is between recording and executing. A payment recorded here is a Manager writing down what they paid and how — a cheque number, a card reference — so the store's obligations reconcile against a bank statement. Nothing in this flow instructs a bank, a processor, or anyone else to move funds. The same distinction governs the till: card tenders are recorded and settled on a separate terminal ([E-05](E-05-sell-a-record.md) d9).

---
| 36 | **Voiding a PaymentBatch raises no ReviewFlag, and payables gains no flag kind. Closes [architecture](../architecture.md) §11's question, as asked rather than by adding a mechanism.** §11 observed that in a single-Manager store the Manager who records a payment is the one who can void it, and nobody is told — and framed it as *the place where "audit matters most where the money is" lands, not as a defect*. **It is not one.** Every payables action is already manager-only ([architecture](../architecture.md) A-28a), so the only people who can do it are the people trusted to; a ReviewFlag exists to tell a Manager what an **Employee** did, and there is no Employee here. **Nothing is lost from the record:** decision 22 appends the void rather than editing or deleting, the original batch stays readable, decision 21 keeps a settled Invoice naming the batches that settled it, and every write carries its actor and timestamp. What is absent is a **notification**, not a trail. [architecture](../architecture.md) A-68's system-raised flags do not change this — a void has an actor, so it was never the kind of thing A-68 widened for. *Accepted consequence, and it is the residual §11 was pointing at:* **in a store with two Managers, one can void the other's payment and the second is not told.** It is recorded, attributable and discoverable in history; nobody is pushed to look. That is judged acceptable at this shop size, and is the thing to revisit if a store ever runs Managers who do not talk to each other |
| 37 | **Voiding or amending an Invoice refuses while any non-voided PaymentBatch targets it. Amends [E-02](E-02-receive-inventory.md) d40 and [E-04](E-04-manage-inventory.md)'s void-and-amend, and resolves the *target has moved on* open question by closing the door rather than answering it.** d40 makes a finalized Invoice correctable **until paid**, and `invoice_is_paid()` already refuses every edit to a **fully** paid one ([architecture](../architecture.md) A-41). The gap was the **partly** paid Invoice: not paid, therefore correctable, therefore amendable or voidable while money stood against it — and decision 22's void would then return that money to a target that no longer says what it said. **Forcing the order removes the case:** void the payment, then correct or void the Invoice, then re-record. Same shape as A-33's *paid blocks the edit* and A-66's *banked blocks the undo* — a reversal is refused while something downstream depends on it, and permitted the moment that dependency is lifted. *Accepted consequence, and it is a real ergonomic cost:* a misread cost on a part-paid Invoice now takes three acts where it took one, and the Manager must retype the targets that were right. **That makes the *re-record pre-fill* open question worth more than it was**, because this decision is what creates the retyping |
| 38 | **Money that ends up in the store's favour always becomes a remainder Credit. One rule, three doors.** An **overpayment** at settlement, a **correction that shrinks an Invoice** below what still-live batches paid against it, and **void money that cannot return to its target** all emit the same artifact. **Resolves the *Overpayment* and *a void that unfreezes an Invoice, followed by a correction that shrinks it* open questions together**, because they were one question asked at two moments. Nothing new is invented: decision 25 already makes a remainder Credit its own artifact and already allows a supplier balance to go negative because of one, and decision 8's *balances are derived, never edited* stays true precisely because the money gets an artifact rather than being absorbed. *Accepted consequence:* an artifact can now appear that **nobody asked for** — a Manager correcting a cost may find a Credit they did not create — and with [M-07](M-07-chart-of-accounts.md) d12 each one writes journal lines. The alternative was a bare negative balance with nothing to click on, which answers *why is this supplier at minus $32* with a shrug |
| 39 | **A clearing is a reversible mark, not a terminal state: a Manager may un-clear a pair, and both rows return to the outstanding list.** Decision 15 already says nothing is deleted or edited *"beyond the clearing fields"*, which is the clue — those fields are the mutable part, and this makes that explicit. **It is safe because a clearing moves no money:** d15 makes it balance-neutral, so it writes **no journal lines at all** ([M-07](M-07-chart-of-accounts.md) d12 has nothing to post), and un-clearing therefore reverses nothing real. No void artifact is built, and **[architecture](../architecture.md) A-36 already agreed** — it gives `ap_payment_batch_voids` and `supplier_claim_voids` and pointedly **no `ap_clearing_voids`**. The act is logged with its actor like every other write ([architecture](../architecture.md) A-4). *Accepted consequence:* **Cleared stops being terminal**, which the [lexicon](../lexicon.md) asserted and A-36's schema never did — the lexicon is corrected in this change rather than the schema |
| 40 | **A re-record may pre-fill from the voided batch it replaces. Decision 18's objection does not reach it.** d18 refused a pre-filled **claim split** because that was decision 11's automatic distribution wearing a different hat — the system inventing a decision nobody had made. **A voided batch is not an invention.** It is what this Manager already chose, minutes ago, and pre-filling replays their own act rather than guessing a new one. The distinction is *whose* decision is being restated, not whether a field arrives populated. Decision 37 makes this worth more than it was: freezing a part-paid Invoice turns void-and-re-record from an occasional correction into the routine way a cost is fixed, so the retyping d22 implied is now the common path rather than the rare one. *Accepted consequence:* a pre-filled form is a nudge toward accepting it, and the reason to void was that **something** in the batch was wrong — so the screen must not let the right-looking rows carry the wrong one through unread |
| 41 | **A duplicate bank reference warns and proceeds.** `Cheque 101` legitimately appears on both a voided batch and its replacement, because it was recorded twice and decision 22 keeps both. But the warning is not really about that case: it catches a **genuine double entry**, the same cheque recorded twice as two live settlements, which is the error worth catching and which nothing else in this flow would see. It does not refuse — decision 5 makes the reference free text precisely because the bank's formats are not the store's to control, and one cheque covering two settlements is a real thing. *Accepted consequence:* the void-and-re-record path, which decision 40 has just made routine, now warns every time on a reference the Manager deliberately reused. **The warning has to say which batch it matched and whether that batch is voided**, or it becomes noise that trains the Manager to click through it |
| 42 | **A credit funds the settlement, not a particular debit. Retires the provisional credit-to-target pairing that building this forced, and resolves the *several credits against several debits* open question by removing the case rather than deciding it.** A target keeps its **Invoice**, its **amount** and its `settle_kind`; it loses the reference naming **which credit** funded it. Decision 18 is untouched — it was always about which **Invoices** a credit lands on, which stays the Manager's — and so is decision 23, which already denied them the *total*. **The pairing turned out to carry information nobody reads.** [architecture](../architecture.md) A-37 already attaches a credit to **at most one non-voided batch**, so decision 22's void un-consumes it from the **batch** and never needed the pair; and an Invoice's balance reads a target's amount and kind, not its provenance. Only A-37's per-credit equality depended on it, and decision 24's whole-or-nothing application is what makes that equality derivable without it — *"whole consumption removed the arithmetic"*, as A-37's own text says. **The failure found the hard way was caused by the pairing:** attributing every credit target to the first ticked credit left the second un-consumed and emitted a remainder for its whole value, moving a balance decision 26 forbids moving. Under this decision both credits are consumed by the batch and the case cannot arise. *Accepted consequence:* [architecture](../architecture.md) **A-36 and A-37 must be amended** before this is buildable, and the prototype carries a working implementation of the shape being retired |
| 43 | **Where the ticked credits are worth more than the supplier is owed, they draw down in the order they were ticked, and the settlement says which one is being partly applied before it commits. Completes decision 42.** d42 removes the credit-to-**Invoice** pairing; it does not remove **all** ordering, because decision 23 applies a credit *"in full, or to the limit of what the supplier is owed, whichever is smaller"* and [architecture](../architecture.md) A-36 requires **one remainder per source credit** — so with `$80` of credits against `$60` owed, something has to say which credit overflows. **Tick order, because the Manager already expressed it** and can change it by re-ticking; no new control is introduced. **Explicitly not oldest-first**, which is a better bookkeeping habit and is also decision 11's automatic distribution wearing a different hat — decision 18 retired that reasoning and it should not return on a second axis. **Explicitly not a refusal** either: decision 25 exists because excess credit is a real thing that needs an artifact, not an error. *Accepted consequence:* the order is only as visible as the screen makes it, so **the settlement summary must name the credit being partly applied and the remainder it will emit**, before the Manager commits rather than after |

## Open questions

- **Aging — the report, not the figure.** Per-Invoice aging is now answerable and is answered: an Invoice carries a due date ([E-02](E-02-receive-inventory.md) d45), so a balance can be shown as overdue and by how much. What stays open is the **report** — balances grouped 30/60/90+ — and specifically whether it lives here or in a general reporting surface. *This question was previously worded as though only its home were undecided. It was not: until d45 there was no due date anywhere in the system, so nothing could be called overdue at all, and days-since-invoice is outstanding rather than overdue. The prerequisite was a field two other flows own, and it is now recorded as [M-01](M-01-supplier-margin.md) d19 and [E-02](E-02-receive-inventory.md) d45.*
- ~~**Reversing a settlement**~~ — **Resolved** by decision 22: a PaymentBatch is voided whole, appended rather than edited, returning money to the balance and credit to the claim's remaining.
- ~~**What a remainder Credit clears against.**~~ — **Resolved** by decision 27: it attaches to debits in a settlement like any other credit, and is consumed when it does. It needs no pairing entry and no widening of decision 15.
- ~~**One remainder per source credit, not per settlement**~~ — **Settled and recorded in [architecture](../architecture.md) A-36** — provenance is a single reference back to the credit it came from. It was never open; struck so the list stops implying it was.
- ~~**Void legality across a remainder chain.**~~ — **Resolved** by decision 30: a void appends a reversing Adjustment rather than reclaiming the remainder, so it never refuses and order stops mattering. There is no chain to walk.
- **Does a void auto-un-clear the target it returns money to?** Decision 37 shuts the Invoice door, leaving one: a **manual ledger entry** that was cleared under decision 15, against which a PaymentBatch is then voided. Money returns to a row marked **Cleared** — retired from attention — which is now a row carrying a balance that nothing will show the Manager. Decision 39 makes un-clearing possible, so the question is only whether the void does it **automatically** or leaves it to the Manager to notice, and *nothing will show them* is the reason that matters. Not blocking: the money is recorded and attributable either way.
- ~~**Several credits against several debits in one selection**~~ — **Resolved** by decision 42, which removes the case: a credit funds the settlement rather than a particular debit, so there is no pairing to decide. The provisional drawdown order is retired with it, and the failure recorded here was caused by the pairing rather than by the order.
- ~~**Is a credited amount necessarily the claim total?**~~ — **Resolved** by [E-04](E-04-manage-inventory.md) d20: the credit memo is the point of truth and its amount is captured when the claim is marked Credited. That amount is what this flow counts, attaches and consumes.
- ~~**Reversing a clearing**~~ — **Resolved** by decision 39: a Manager may un-clear, and both rows return to the outstanding list. No void artifact is built because a clearing moves no money and writes no journal lines.
- ~~**Voiding a payment whose target has moved on — amended, voided, or cleared**~~ — **Resolved** by decision 37, which closes the door rather than answering it: an Invoice cannot be amended or voided while a live PaymentBatch targets it, so the collision cannot arise. **The manual-entry door is narrower but not shut** — see the refined question below.
- ~~**A void that unfreezes an Invoice, followed by a correction that shrinks it**~~ — **Resolved** by decision 38: the correction emits a remainder Credit, and decision 8's prohibition on editing the balance stays intact because nothing absorbs the money silently.
- ~~**Does a claim come back onto the list when a void refills it?**~~ — **Resolved** by decisions 24 and 22 together: a claim is binary, so a void returns it to un-applied whole and it reappears exactly as it was. There is no partial state to reason about.
- ~~**May a re-record pre-fill from the voided batch?**~~ — **Resolved** by decision 40: yes. d18's objection was to the system inventing a distribution; a voided batch is the Manager's own prior choice, not an invention.
- ~~**A duplicated bank reference**~~ — **Resolved** by decision 41: it warns and proceeds, and the warning names the matched batch and whether it is voided. The case worth catching is a genuine double entry, not the void-and-re-record one that raised it.
- ~~**Where the sum of a claim's applications is bounded**~~ — **Resolved** by decision 24: there is no sum to bound. A claim is applied whole, so the concurrency question is a status check rather than a running total held under a row lock.
- ~~**Overpayment**~~ — **Resolved** by decision 38: yes, an overpayment emits a remainder Credit, exactly as an excess claim credit does. It was the same question as the one above, asked at a different moment.
- ~~**Prepaid and COD.**~~ — **Resolved** by decision 35: a Prepaid supplier may carry a balance, and it is a prepayment not yet recorded rather than a bill outstanding. It counts, it ages nowhere, and it is settled by a Manager confirming a pre-filled settlement — never by finalizing the Invoice. *What the resolution does not cover:* the system cannot see a mismatch between what was prepaid and what was invoiced, because it never knew the first figure. That gap is worked by hand through decision 12, not detected.
- **Terms on a manual ledger entry.** Decision 12 makes a manual entry a lump not sourced from Receiving, so it inherits nothing from an Invoice. Whether a hand-entered Invoice or Consignment should nonetheless take the Supplier's payment terms — and therefore age — is unsettled.
- **Deeper reconciliation.** v1 is manual throughout. Auto-matching a supplier's credit note to the original claim and recording disputes are both later. *Partial credits are no longer part of this question — decisions 24 and 25 dispose of them: a claim is applied whole and any excess becomes its own artifact.*
- **Receivables.** [E-07](E-07-manage-customers.md) allows a business customer to owe the store money on an outbound invoice, but nothing chases it. The receivables side of this flow doesn't exist yet.
- ~~**Currency movement.**~~ — **Closed by [M-07](M-07-chart-of-accounts.md) d17, by placing the gain rather than computing it.** A journal line carries its amount and its own currency code and nothing is ever converted, so a USD Invoice exports as USD. The accountant converts, and the difference they find **is** the exchange gain — which [M-07](M-07-chart-of-accounts.md) d1 puts with whoever holds the books, and that is not this system. **The bill [M-06](M-06-settings.md) d39 warned of is not owed:** A-36, A-47 and d37 all stand untouched, and decision 37's refusal to sum across currencies is unaffected. *The original question follows.*
  *Original:* An Invoice raised at one rate and paid at another produces an exchange gain or loss that nothing records. **[M-06](M-06-settings.md) d39 places it with the chart of accounts, not with settings:** a gain or loss is a ledger posting, this system has no general ledger, and computing one now would produce a figure with nowhere to post it. When a ledger arrives the mechanism is to record the rate **on the artifact that used it** — the shape [architecture](../architecture.md) A-36 and A-33a already take — not a table of historic rates. Summing across currencies stays refused meanwhile (M-06 d37).
- ~~**Payment batches**~~ — **Resolved** by decision 16: one PaymentBatch per Record Payment action, opened to see its targets.
- ~~**Partial claim credit**~~ — **Resolved** by decision 24, not by decision 20 as first written: a claim is **not** partially applicable. It is applied whole, and an excess over what is owed becomes a separate remainder Credit (decision 25). Decision 23 removed the case that made partial application look necessary.
