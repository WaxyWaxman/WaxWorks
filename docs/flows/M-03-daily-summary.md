# M-03 — Daily summary of sales and inventory

**Actor:** Manager (Undo End of Day is **manager-only**, not a retired *manager override* — [architecture](../architecture.md) A-28a)
**Status:** Specified — surfaced on **Point of Sale** under **Other Functions** ([E-05](E-05-sell-a-record.md) decision 30), not a screen of its own
**Related:** [E-05 Point of Sale](E-05-sell-a-record.md) · [E-06 Process a return](E-06-process-a-return.md) · [E-07 Manage customers](E-07-manage-customers.md) · [M-06 Settings](M-06-settings.md) · [M-07 Chart of accounts](M-07-chart-of-accounts.md) · [M-08 Keep the general ledger](M-08-general-ledger.md)

**Job:** As a manager, I need an end-of-day picture of what sold and what the stock position looks like.

---

## The close

Closing the day is a real state transition, not just a report: it moves every **Current** Sale to **Closed**, after which those Sales are no longer editable ([E-05](E-05-sell-a-record.md)).

1. Manager runs **View Subtotal** as often as they like during the day. It produces the same breakdown as the close and changes nothing.
2. Manager runs **Total Today's Sales** to close. The system produces the breakdown and moves all Current Sales to Closed.
3. The closed group is a **batch**, carrying its own identifier, the timestamp it was run, and the User who ran it.
4. **Undo End of Day** reverses a batch, returning its Sales to Current. It is **manager-only** ([architecture](../architecture.md) A-28a, and decision 4 below): on a store session a Manager or Owner authorizes in place by entering their **PIN**, and both names are recorded — the Manager's and the Employee whose session it is; on a personal session the session itself is the authorization ([E-01](E-01-authenticate.md) d26, d27; [M-04](M-04-manage-users.md) d3 as amended, M-04 d4, M-04 d31). It is not a *manager override* — that term is retired ([M-04](M-04-manage-users.md) d8), and it never covered this action.

A batch is not the same thing as a calendar day. Sales rung after a close belong to the next batch even if the date hasn't changed, and a shop that closes twice in a day produces two batches.

---

## What the summary contains

**Eight named sections, in a fixed order** (decision 17). The first is the report's own
provenance; the next four are the money; the last three are the counter. Every figure
below the *Tenders/Misc* block is **tax-exclusive** — built from line nets, as the close
has always been — and *Tenders/Misc* and *Tendering details* are the two that **include
tax**, because a tender is money that moved.

| # | Section | Contents |
|---|---|---|
| 1 | **File info** | The **start** timestamp (first record in the batch), the **end** timestamp (last record), and the **created** timestamp — when this report was generated. Bounds the **batch**, never the calendar day (decision 18) |
| 2 | **Sales** | Per **Section code** ([M-06](M-06-settings.md) d28): item count, retail value, discount given across those items, and net. A matched Return **nets into its own Section**. Footer: total items, total retail and net, the retail value of discounts and what **percentage of total sales** they are, the transaction count, and an **average sale** (decision 19) |
| 3 | **Taxes** | Per tax collected: how many times it was collected, its **rate**, the **taxable base** — the value of the items that bore it — and the tax taken. Splits **by rate within its type** where the period spans a rate change (decision 15, preserved by decision 20) |
| 4 | **Tenders/Misc** | Per tender: the **GL account it posts to** ([M-07](M-07-chart-of-accounts.md) d21, d26), how many transactions used it, and the total taken. Then an **Undeposited funds total** over the tenders that post there — cash and card — and beneath it a line per tender posting elsewhere. `Cash — pay-outs` and the `Cash, net` subtotal survive unchanged (decisions 14, 16, 21) |
| 5 | **Sales by customer type** | The same figures as section 2, split by the Customer's **Account type** — Regular, Staff, Business — plus **Walk-in** for a Sale carrying no Customer (decision 22) |
| 6 | **Sales per hour** | Transactions and value of items sold, per hour. An hour with no activity **carries no line** — a shop open 11–6 prints seven. A **void count** rides each hour (decisions 23, 26) |
| 7 | **Sales by Employee** | Per Employee: transactions completed, items, the value of those items, the **average discount** offered, returns and their value, and a void count. Attributed by [E-05](E-05-sell-a-record.md) d23 — whoever held the lock at tender (decision 24) |
| 8 | **Tendering details** | Per tender type, **every transaction in the period**: the Employee who tendered, the transaction number, the Customer's name — **Walk-in** where there is none ([E-05](E-05-sell-a-record.md) d30) — and the amount (decision 25) |

The **stock position** — Records below their minimum on hand — is unchanged and sits
below the eight, because it is the one block that is not about the period at all.

Returns and pay-outs appear as **negative amounts against the tender they moved through**, not netted silently into gross sales — a $200 day with a $50 refund reads as $200 gross, −$50 returns, $150 net, not as $150 of sales.

For both of them the tender they moved through is **cash**, so both reduce the cash figure (decision 16). They keep their own labelled rows — `Cash — pay-outs` beside `Cash` — because decision 14 reports every movement rather than a net one, and a **`Cash, net`** subtotal sits beneath them. That subtotal is what moved, **not what is in the drawer**: there is no opening float here, for the reason given below.

---

## Over a date range

The same report runs over an **arbitrary date range** — this month, last month, year to
date — and not only over a batch (decision 27). It carries **seven of the eight
sections**: everything except **Tendering details**, which over a year is tens of
thousands of per-transaction lines and is the one section nobody reads at a glance.

Two things change shape rather than staying identical:

- **File info**'s start and end are the **range's** bounds rather than the batch's, and
  it names how many batches fall inside them — a range that caught a day nobody closed
  is a range whose figures are short, and the count is where that shows.
- **Sales per hour** becomes an average **per hour of the day** across the range, so a
  month reads as twenty-four rows at most rather than seven hundred.

---

## What it deliberately does not contain

**No cash-drawer reconciliation.** There is no opening float, no counted-versus-expected comparison, and no over/short figure. The summary reports what the system was told happened; comparing that against the physical drawer is done outside the system by choice.

Pay-outs are the one cash movement that is captured, because money leaving the till for an expense is otherwise invisible to every other report.

---

## Requirements

- Every Sale in a batch must be attributable to the batch, and every batch to the User who closed it.
- Undoing a close must restore Sales to exactly the state they held before it, including their Sale numbers.
- The breakdown by Section depends on every sellable thing carrying a Section, including non-tracked items like freight ([E-05](E-05-sell-a-record.md)).
- Margin figures must carry the E-02 caveat below wherever they appear.
- **Every Sale must carry a tender timestamp**, which sections 1 and 6 both key on ([architecture](../architecture.md) A-73).
- **A void must be attributable to a batch and to an Employee**, or sections 6 and 7 report a count over the wrong period. The prototype currently scopes voids to everything in memory rather than to the batch (`prototype/src/lib/dayBreakdown.ts`), which is acceptable in a single-session demo and is not acceptable here — a void count that includes last week's is worse than no void count.
- **A retail value and a discount must be separable on every Sale line.** Sections 2, 5 and 7 all report them as two figures, so a line storing only its net cannot produce either.
- Decision 27's range report requires that **decision 13's stored summary carries every figure sections 2–7 report**, not only the ones the close displays today. A figure omitted from the stored summary is a figure no range can ever show.

---

## Inherited from other flows

**From [architecture](../architecture.md) A-83 and A-84:**

- **The stored summary is the schema of every range report, and it carries a version.** Decision 27 sums decision 13's stored
  summaries, so `close_batches.summary` is the only source a range has: a figure this flow omits from the close is one **no range
  can ever show**, for every batch already written. `close_run` stamps a **schema version** and every reader keys on it, so a
  range spanning two shapes reports each figure over the batches that carry it and **prints the count of those that do not** —
  the same move decision 27 already makes with its batch count. **Zero-filling is refused**, and **recomputation from source
  Sales is not offered**: A-30 stores precisely so that no second path exists, and a silent recompute is the quiet restatement of
  a past day it refuses. *What this costs this flow:* adding a ninth section later changes what every **future** range can show
  and nothing before it.
- **Decision 4's batch half is settled: an undo retires its CloseBatch and re-closing writes a new one.** Decision 4 restores the
  **Sales**, numbers included, and said nothing about the **summary** — and nothing made a stored summary immutable, so a
  re-close could already have rewritten a past day's figures. A retired batch keeps its id, timestamp, closing User and the
  summary it computed, gains the undo's actor and both names, and **nothing sums it**: decision 27's range reads live batches
  only. *Accepted consequence:* one calendar day can carry several CloseBatches, only one live.


**From [E-06](E-06-process-a-return.md) decision 26:**

- **Two kinds of return line now report differently.** A **matched** Return reverses its own sale and reduces gross
  sales as before. An **unmatched** one — [E-06](E-06-process-a-return.md) decisions 24 and 25, a disc the shop has no
  sold record for — is booked as a purchase and **does not reduce gross sales**; it raises Inventory instead.
  [E-06](E-06-process-a-return.md) decision 8 is untouched: both still show their money in the **tender column**
  rather than netting into gross, so the column still reconciles.


**From [E-06](E-06-process-a-return.md) decision 29 — the state this note governed no longer exists:**

- ~~**An unrouted returned copy does not hold up the close.**~~ — **Moot under
  [E-06](E-06-process-a-return.md) decision 29.** A Return can no longer be finished with a returned line
  undecided, so a **finished** Return with unrouted stock cannot reach a close at all and the close has nothing
  to decline to wait for. *The commitment is retired rather than reversed:* the close still never refuses on
  stock, because there is no longer any stock in that state to refuse on. **What replaces it is stronger and
  belongs here:** every copy a Return took in has reached its destination **before** the close runs, so the
  Inventory figure the close journals and the copies on hand agree on the day rather than the morning after —
  which is what closed [M-07](M-07-chart-of-accounts.md)'s open question about the overnight gap.
  *The retired note follows.* [E-06](E-06-process-a-return.md) decision 20 created a Return that is finished
  with its copy not yet assessed, and decision 23 let that copy be routed after the day had closed — a Return
  taken at five o'clock is usually looked at the next morning. The close neither waited for it nor refused on
  it: the copy was off the shelf either way, so there was nothing for the close to reconcile.


**From [M-08](M-08-general-ledger.md):**

- **Undo End of Day must refuse while the day it would reopen sits in a sealed period** ([M-08](M-08-general-ledger.md) d11, d22, d29, [architecture](../architecture.md) A-75). Decision 4's undo is the one reversal in this system that does **not** post forward — it un-closes, returning Sales to Current and restating the day itself — so unlike an Invoice correction ([E-02](E-02-receive-inventory.md) d40) or a PaymentBatch void ([M-05](M-05-accounts-payable.md) d22), it cannot be absorbed by [M-07](M-07-chart-of-accounts.md) d8's post-forward rule. **This is [architecture](../architecture.md) A-66's shape with a second trigger** — A-66 already refuses `close_undo` while a non-voided BankDeposit references the batch, and a sealed period is the same kind of outside dependency, one step further out. **A sealed month releases**: unseal it and the undo becomes available again — though only the **most recently sealed** period may be unsealed ([M-08](M-08-general-ledger.md) d29), so reaching an older day means walking backwards a period at a time. **A year marked *filed* never releases** (d22, superseding d21) — so a day inside a filed year can never be reopened by anyone, which is the only permanently unreachable state in this flow and worth the refusal saying so in those words rather than offering a route that does not exist.

- **A day *before* the books open is protected by nothing, and stays undoable** ([M-08](M-08-general-ledger.md) d45). The rule above is about a **sealed** period; step 16 of M-08 only ever offers months from the opening position forward, so a day earlier than that can never be sealed and Undo End of Day stays available on it indefinitely. **This is deliberate rather than an omission:** M-08 d45 puts those days' journal lines outside what any statement reads, so restating one moves no reported figure. It would stop being harmless the moment a statement began reading them, which is the thing M-08 d40 forecloses.

**From [E-02](E-02-receive-inventory.md):**

- **Margin figures reflect supplier cost only.** Freight, tax, and miscellaneous costs stay at the Invoice level and are not allocated to items, so per-item margin is slightly overstated — roughly 2.6% on the reference Invoice. Any margin reporting here states the caveat.

**From [E-05](E-05-sell-a-record.md):**

- Closing moves Current Sales to Closed; a Closed Sale can only be reopened by undoing its batch.
- Pay-outs and `Used Credit` are tender types and appear in the tender breakdown.

**From [E-04](E-04-manage-inventory.md):**

- Minimum on hand is informational and surfaces here rather than raising orders.

**From [M-06](M-06-settings.md):**

- **Tax is reported per tax *type*** — GST, QST — rather than per tax line ([M-06](M-06-settings.md) d11). Where a line carried two taxes, each is reported against its own type: the split is what gets remitted, and two composition orders that agree on the customer's total can disagree on it (M-06 d16).
- **The *By tender* breakdown reports configured tenders, not behaviors** ([M-06](M-06-settings.md) d22). `Visa`, `Mastercard` and `Amex` are three rows, not one `card` row, because they usually settle as three separate bank deposits and a merged figure cannot be tied back to a statement.
- **Cash rounding is its own tender** ([M-06](M-06-settings.md) d26, amending d21), behavior `rounding`, written by the system and never selectable. It carries `−$0.02` when a cash tender rounds up and `+$0.02` when it rounds down, so a Sale's tenders sum to its total and the rounding tallies through the *By tender* breakdown with no line invented for it — decision 14's reasoning reaching its natural end. It is also what makes decision 8's refusal to reconcile the drawer a choice rather than an oversight.
- **Sections carry a sort order** ([M-06](M-06-settings.md) d28) which the *By Section* breakdown follows, and are an editable table rather than a fixed pair of values.
- **Whether a Section enters the *By Section* breakdown is a flag on the Section** ([M-06](M-06-settings.md) d20), not a hard-coded exclusion. Freight and services are revenue and appear; the gift-card Section is a liability and does not — which is decision 14 restated as configuration rather than as a special case in this flow.
- **Merging a Genre moves every Record under it in the *By Section* breakdown at once** ([architecture](../architecture.md) A-60, [M-06](M-06-settings.md) d31, d32). A Record's Section is derived from its genre's required parent rather than stored, so a merge into a genre with a different parent re-buckets everything beneath it in one act. **Completed Sale lines are unaffected** — they keep what they snapshotted (M-06 d8), so a past day's summary does not restate.
- **Gift card loads resolve through a system-owned catalog entry** ([M-06](M-06-settings.md) d18) which, like every sellable thing, carries a genre and therefore a Section. **That Section is excluded from the *By Section* breakdown** — decision 14 is unchanged and governs: a load is money in but not a sale, so it stays out of gross and out of Section, and is reported as an *of which* line against the tender that took the money.

**From [M-07](M-07-chart-of-accounts.md):**

- **The close writes a journal batch onto the CloseBatch, beside the summary decision 13 already stores there** ([M-07](M-07-chart-of-accounts.md) d7). Its lines group by **`(business date, account)`**, not by account alone ([M-07](M-07-chart-of-accounts.md) d14) — because step 2 closes **all** Current Sales rather than today's, so a close nobody ran on Monday would otherwise report Monday's revenue on Tuesday, which is harmless most weeks and wrong across a month boundary. If debits and credits disagree the difference posts to **Suspense** and a ReviewFlag is raised; **the close proceeds either way** ([M-07](M-07-chart-of-accounts.md) d10).
- **Undo End of Day refuses while the batch has been banked. Amends decision 4** ([architecture](../architecture.md) A-66). A non-voided BankDeposit referencing the CloseBatch blocks `close_undo`; voiding that deposit releases it. A-33a's shape with a different trigger — **immutable while banked**, never immutable forever. Decision 4's restore-exactly guarantee is unchanged for every batch that has not been banked.

**From [architecture](../architecture.md) A-86 and the user model (2026-09-20):**

- **Undo End of Day is authorized by PIN on a store session and by the session on a personal session; both names are recorded on a store session** ([E-01](E-01-authenticate.md) d26, d27; [M-04](M-04-manage-users.md) d31). *Enters their own initials* is retired wording (E-01 d29).
- **Sales by Employee names people whose initials are unique per Store, not per Organization** ([E-01](E-01-authenticate.md) d25) — the section resolves to the **name** ([M-04](M-04-manage-users.md) d16) and, since the close is per Store (A-86 keeps CloseBatches per Store), never has to disambiguate two Stores' people on one page. Consolidated views across Stores stay deferred ([architecture](../architecture.md) A-72, A-86).

---

## Resolved decisions

| # | Decision |
|---|---|
| 1 | The close is a state transition — Current Sales become Closed and stop being editable |
| 2 | A closed group is a **batch** with its own identifier, timestamp, and closing User |
| 3 | A batch is not a calendar day; two closes in one day produce two batches |
| 4 | **Undo End of Day** is manager-only and restores Sales to their prior state, Sale numbers included |
| 5 | **View Subtotal** produces the same breakdown without closing anything |
| 6 | The summary breaks down by **Section** and by **tender type** |
| 7 | Returns and pay-outs show as negative amounts against their tender, never netted into gross |
| 8 | **No cash-drawer reconciliation** — no float, no counted-versus-expected, no over/short |
| 9 | Pay-outs are captured with their notes, being otherwise invisible cash movements |
| 10 | Records below minimum on hand are reported here |
| 11 | Margin figures carry the E-02 unallocated-cost caveat |
| 12 | **Held Sales are absent from the close.** The close touches only Current Sales — a Hold is not revenue until tendered. Held copies still reduce *available* stock, so the stock position reflects them. Closes the Held-Sale open question ([architecture](../architecture.md) A-23) |
| 13 | **The summary is stored on the batch and printable.** `close_batches.summary` holds the computed breakdown at close time; a print route renders it. Storing rather than recomputing means an Undo End of Day cycle can never quietly restate a past day. Closes the delivery open question (A-30) |
| 14 | **The tender column reports every movement of money, not a net figure.** Refines decision 6. **Account Balance** is split by direction — a customer paying onto their account and a customer spending that credit are opposite movements that happen to share a tender type, and netting them reports `$0` for a day that did $100 of each. **Gift-card loads** are reported as an *of which* line against the tender that took the money: loading a card is money in but not a sale ([M-05](M-05-accounts-payable.md) decision 10), so it stays out of gross and out of Section, and naming it is what lets the tender column be reconciled against net sales instead of silently exceeding it |
| 15 | **Where a reporting period spans a rate change, tax is broken out by rate within its type.** A period covering 1 April shows GST at 5% and GST at 6% as two figures rather than one, because a remittance return generally wants the base and the tax **per rate** — a single GST line is the one report a shop hands to an accountant, and it is the one that would hide the change. Extends the per-type reporting this flow already does (d13, [M-06](M-06-settings.md) d11) rather than replacing it: **one figure per type is still what a normal period shows**, and the split appears only when more than one rate actually contributed. **It costs nothing to store.** The rate is already on the Sale line — [architecture](../architecture.md) A-57 and §5 snapshot *the rates applied* — so this is a grouping over data the close already reads, not a lookup against `tax_types` and not a reason to keep a rate history. *Accepted consequence:* the close's tax block changes shape on exactly the days it matters, which is the point and will still surprise whoever sees it first |
| 16 | **A pay-out is reported against cash, and the tender block carries a `Cash, net` subtotal. Completes decision 7 and the section above; the built prototype did neither.** The body of this flow has always said it — *"returns and pay-outs appear as negative amounts against the tender they moved through"* — and a pay-out moves through **cash**; [E-05](E-05-sell-a-record.md) d16 says it in as many words, *"appears as a negative cash line in the M-03 close"*. What was built gave the pay-out a tender row of its own, so **a day whose only cash movement was a $20 pay-out carried no cash line at all**, and the money that left the drawer read as a category nobody reconciles rather than as cash going out. A **cash refund was already right** and is untouched: [E-06](E-06-process-a-return.md) tenders it as a negative `Cash` tender, so it has always netted here — the pay-out was the one out of step. The row is labelled **`Cash — pay-outs`** rather than folded silently into `Cash`, because decision 14 holds that the tender column reports **every movement of money, not a net figure**, and that rule is what makes a day of $100 in and $20 out legible. The **subtotal sits beside the movements rather than replacing them**, and is a subtotal rather than a row, so nothing that sums the column double-counts it. *Accepted consequence, and the wording has to keep carrying it:* this flow holds **no opening float** and runs no counted-versus-expected comparison, so `Cash, net` is **what moved, never what is in the till** — it is the figure a person counts the drawer against, and the counting stays outside the system exactly as *"What it deliberately does not contain"* says |
| 17 | **The summary is eight named sections in a fixed order, and the order is the argument.** The close has grown from six unlabelled blocks into a document, and a document that reorders itself cannot be read twice the same way. **File info first**, because a report with no stated bounds is a figure with no claim attached — the commonest way to misread a close is to read yesterday's. Then the **money in the order it is asked about**: what sold, what tax rode on it, what came through the drawer. Then the **counter** — who bought, when, from whom, and every tender line behind it. **Sections 2 and 5 share one arithmetic deliberately** (decision 22): the same footer under two different splits is what makes them comparable, and a reader who has learned to read one has learned to read both. *Accepted consequence:* this is a **long report**, and the day-to-day question — *what did we take?* — now sits above seven sections nobody reads most days. Decision 28 answers that with collapsing rather than by shortening the report, because the sections that are noise on a Tuesday are the whole point on the day something is wrong |
| 18 | **File info bounds the *batch*, never the calendar day.** Decision 3 has always held that a batch is not a day — a Sale rung after a close belongs to the next batch even when the date has not changed, and a shop closing twice produces two — so *"the first record on this day"* names a boundary this flow does not have. **Start** is the tender timestamp of the earliest record in the batch, **end** the latest, and **created** is when the report was generated, which is the one of the three that differs between **View Subtotal** and **Total Today's Sales**: run the subtotal four times and it carries four different created stamps over the same start and end. That is the point of carrying it — a printed tape found on the counter says **when it was true**, and decision 13's stored summary and a fresh subtotal over the same Sales are otherwise indistinguishable on paper. **Start and end are the data's own bounds, not the period's** — an empty batch has no first record, so both read as em dashes rather than as the close's timestamp, and a batch opened at nine whose first Sale is at noon starts at noon. *Accepted consequence:* the report does not state the hours the shop was **open**, only the hours it **rang something**, and those differ on a slow day |
| 19 | **The Sales block is per Section code, a matched Return nets into its own Section, and the footer carries the discount ratio and the average sale.** **Per Section *code*** ([M-06](M-06-settings.md) d28's two-character code) beside the name, following d28's own reason for carrying a code at all — a code is what a column can be narrow enough to print. **Netting the Return into its Section** is what makes the block reconcile: the section rows now sum to **net sales**, the figure this flow has reported since decision 7, and a returned `VINYL` copy reduces `VINYL` rather than landing in an *of which* row that belongs to no Section. *Rejected:* returns as their own columns beside retail and net, which reads better for a Section with heavy returns and costs the reconciliation — and returns per Employee, which is where a returns pattern is actually watched, are section 7's job. **Four figures per row** — items, retail, discount, net — where `net = retail − discount`, and **retail is the pre-discount extended price**, so the discount column is a real subtraction rather than a restatement. **The footer adds three figures the close has never had**: the discount total **as a percentage of total sales**, which is the one number that says whether the counter is giving the shop away; the transaction count, which the old *Sales* block already carried; and an **average sale**, `net ÷ transactions`. **A `$0.00` Sale counts as a transaction** — [E-05](E-05-sell-a-record.md) step 14's counter buy is rung on one — so the average is dragged down by trade-ins on a day with many, and that is correct rather than a flaw: those are transactions the counter worked. **What stays out**: a gift-card load, whose Section carries *counts as revenue = false* ([M-06](M-06-settings.md) d18, d20) and which decision 14 already keeps out of gross; and an **unmatched Return** ([E-06](E-06-process-a-return.md) d26), which is a purchase rather than a reversal and reduces neither. *Accepted consequence, inherited and worth repeating here:* [E-07](E-07-manage-customers.md) d22 rings a pre-migration paper credit note as a **discount**, so during the changeover this block's discount figure and its percentage both read high for reasons that have nothing to do with the counter — the block says so where the percentage is printed |
| 20 | **The tax block gains a collection count and the taxable base. Decision 15's rate split is preserved exactly and is what the base is grouped by.** The close has reported *tax collected per type* since decision 13; a remittance return wants **the base and the tax**, which decision 15 already said in as many words — *"a remittance return generally wants the base and the tax per rate"* — while reporting only half of it. So this is d15's own reasoning finished rather than a new want. **The base is the value of the items that bore that tax**, which on a compounded cell is **not the same base twice**: [M-06](M-06-settings.md) d16's `ab+` charges `b` on subtotal **plus** `a`, so `b`'s base is larger than `a`'s by exactly `a`, and printing one shared base would misstate one of two figures remitted to **different authorities**. Each tax line therefore carries **its own** base — the base it was actually charged on. **The count** is how many times that tax was collected, counted **per Sale line**, not per Sale: a Sale of three taxable items and one exempt one contributes three, because the count exists to be read against the base and a per-Sale count could not be. **Where a rate change splits a type** (d15), every column splits with it — `GST (5%)` and `GST (6%)` carry their own count, base and tax — since a base straddling two rates is precisely the figure d15 exists to prevent. *Accepted consequence:* the block is four columns where it was two, and on an ordinary day three of them tell the reader nothing they could not have divided out themselves. They are there for the quarter, not for the Tuesday |
| 21 | **Tenders/Misc names the GL account behind each tender and groups by it. The grouped total is *Undeposited funds*, not a bank total. Decisions 14 and 16 are preserved whole.** Naming the account on the report is what makes the block reconcilable against the books without holding both open: [M-07](M-07-chart-of-accounts.md) d21 already decided **each tender's account is implied by its behaviour**, so the column is a lookup this flow performs rather than a fact it invents. **The grouped total is the one thing the original proposal had wrong, and the correction matters.** Cash and card do **not** post to a bank chequing account: d21 lands them in **undeposited funds**, and money reaches the bank only through a **BankDeposit** ([architecture](../architecture.md) A-66, which is already load-bearing here — a non-voided BankDeposit referencing the batch refuses decision 4's undo). So the total over cash and card is *what is on its way to the bank*, and calling it a bank total would make it read as a balance the bank already agrees with. **Beneath it, one line per tender posting elsewhere**, each under its real account: `Used Credit` → **Second-hand purchases** ([M-07](M-07-chart-of-accounts.md) d26, which amended d21 specifically on this — what a counter buy debits is the goods); a gift-card redemption → the **gift card liability**; `Store Credit` → **customer account credit**. The [lexicon](../lexicon.md) forbids calling any of that *accounts receivable* — store credit is a **liability**, money the store owes. **Decision 14 is untouched**: `Account Balance` keeps its direction split, because a day doing $100 of top-ups and $100 of draw-downs must not read `$0`. **Decision 16 is untouched**: `Cash — pay-outs` keeps its own row beside `Cash`, with the `Cash, net` subtotal beneath them, and both sit **inside** the undeposited-funds group since a pay-out is cash that never got there. **Values include tax**, alone with section 8 in doing so, because a tender is money that moved and money that moved carried the tax with it. *Accepted consequence:* the block now shows account names a Manager did not choose and cannot edit here — they are [M-07](M-07-chart-of-accounts.md)'s, renamed there and nowhere else — so a reader who renumbers their chart sees this report change without having touched it |
| 22 | **Sales by customer type splits on the Customer's Account type, with Walk-in as the fourth bucket. The field already existed.** [E-07](E-07-manage-customers.md) carries **Account type** — *Regular, Staff, Business* — and d17 already bands the customer list by it, so this needs no new field and no new decision from that flow: four rows, the first three from the Customer and **Walk-in** for a Sale carrying none. *Walk-in* rather than *unlinked* or *none*, because [E-05](E-05-sell-a-record.md) d30 already settled that word for exactly this case on the till rail, and a second word for one idea is drift. **The figures are section 2's, unchanged** — items, retail, discount, net, and the same footer — which is the whole reason the split is worth having: **Staff** as its own row is the one that earns it, since staff purchases are the discounts nobody audits and they have never been separable before. *Explicitly not a customer-type table of its own:* there are three values, they live on the Customer, and inventing a typed entity here would be a schema for a dropdown. **If [E-07](E-07-manage-customers.md) ever adds a fourth type it appears here with no change to this flow**, which is the point of splitting on the field rather than on a rule. *Accepted consequence:* Account type is now a **reporting dimension**, so changing a Customer's type moves their whole history in this report at once — the same shape as [M-06](M-06-settings.md) d31's genre remap moving Records between Sections, and it will surprise someone the same way |
| 23 | **Sales per hour prints only the hours that rang something, keyed on the tender timestamp.** The hour is the hour of the **tender** — the moment the money moved — which is the timestamp [architecture](../architecture.md) A-73 already names as the Sale's own and the one A-57 resolves tax at, so no second notion of when a Sale happened is introduced. The day boundary is **midnight** with no configurable business day ([M-07](M-07-chart-of-accounts.md) d19), so a Sale rung at 00:30 files in the 00 hour of the following day and the block inherits that rather than arguing with it. **An hour with nothing in it carries no line**: a shop open 11–6 prints seven rows, not twenty-four, because twenty-four rows of which seventeen are zero is a table that hides its own shape. *Accepted consequence, and it is the trade:* **a dead hour and a closed hour look identical** — an 11–6 shop that took nothing between 2 and 3 prints six rows, and nothing on the report says the shop was open for the missing one. The alternative, printing zeros between the first and last active hour, was rejected as inferring opening hours from sales, which is the same guess wearing a number |
| 24 | **Sales by Employee. Closes the deferred per-employee breakdown.** The open question read *"whether the summary presents that breakdown is a reporting choice, deferred with the rest of trend reporting"* — [E-05](E-05-sell-a-record.md) d23 unblocked it by making a Sale attributable to **whoever held the lock at tender**, and this decision takes the choice: it is presented, here, and not deferred to a trend-reporting flow that does not exist. **Seven figures per Employee** — transactions, items, the value of those items, the **average discount offered**, returns, the value of returns, and a void count. **The average discount is the figure this section exists for.** Every other column is a volume measure that a busy Employee scores high on by working; the discount average is a **rate**, so it is comparable between an Employee who rang forty Sales and one who rang four, and it is the one number on this report that answers *is someone discounting more than everyone else*. It is `total discount ÷ items discounted`, **not** divided by all items — averaging in the lines that took no discount buries the signal under whoever happened to be on a quiet shift. **Returns are counted and valued here** where decision 19 declined to column them by Section, because a returns pattern is a property of **who took it**, not of what was sold. *Accepted consequence, and it should be said plainly:* this is the first surface in Wax Works that **compares Employees to each other**, and a shop that prints it daily has made a management decision this flow cannot make for them. [M-04](M-04-manage-users.md)'s review queue exists for actions that need a Manager's attention; this is not that, it is a scoreboard, and a figure that is high for an honest reason — the Employee who works the trade-in counter — has nothing on the report to say so |
| 25 | **Tendering details lists every transaction in the period, grouped by tender type. The only section that is a listing rather than an aggregate.** Four columns — the **Employee** who tendered, the **transaction number**, the **Customer's name**, and the **amount** — under a heading per tender type, so the block reads as *here is every card transaction, and here is every cash one*. **Customer name says `Walk-in` where there is none**, following [E-05](E-05-sell-a-record.md) d30, which prints the Customer under the transaction number on the till rail for the same reason it matters here: *"a Sale number is only findable by someone who already knows it"*. **Grouped by tender rather than by Sale**, which means a **split tender appears once per tender** — a Sale settled half in cash and half on a card is a cash line and a card line, both carrying the same transaction number. That is correct and is the reconciliation this block is for: each group sums to its own tender total in section 4, and nothing in the group is a Sale total. **Amounts include tax**, matching section 4 for the same reason. *Accepted consequence, and it is what decision 27 rests on:* this section grows **one line per tender per transaction**, without bound. A busy Saturday prints pages of it, and it is the reason a date-range report does not carry it at all |
| 26 | **A void is counted and never valued.** Sections 6 and 7 both report voids, and both report **how many** rather than **how much**. Considered and rejected: the **retail value of the lines on the Sale when it was voided**, which is the figure that would spot an Employee ringing $400 and voiding it repeatedly, and the **value of tenders struck to allow the void**, since [E-05](E-05-sell-a-record.md) d31 permits a void only at zero and the money must be refunded, moved, or struck first (E-05 d32). Both were declined for the same reason: **a void has no settled value**, and every candidate figure is a number about a transaction that deliberately did not happen. A voided Sale's lines are whatever was on it at the instant it was struck, which an Employee correcting a mis-scan changes three times in a minute — so the figure would be **loudest for the most ordinary mistake** and silent for a void of an empty Sale. The **count** carries the signal without the arithmetic: a pattern of voids is visible as a pattern, and it is visible **per hour** and **per Employee**, which is where a pattern is read anyway. *Accepted consequence, stated because it is the thing this forgoes:* **one large void and one small void are the same line on this report**, so the report can say *someone voided a lot* and cannot say *someone voided a lot of money*. Answering the second means opening the Sale, and nothing here routes you to it |
| 27 | **The same report runs over an arbitrary date range, carrying seven of its eight sections. Resolves the *Arbitrary date ranges* open question, and takes the half of it the question expected to be cheaper.** The question asked whether a range means **summing stored batch summaries** (decision 13) or **querying Sales directly**, and noted the stored summaries make summing the cheaper. **Summing is what this decides — and it is why *Tendering details* is dropped rather than paged.** A stored summary is an aggregate: sections 2 through 7 sum out of one, and a **per-transaction listing does not**, so carrying section 8 over a range would mean either storing every tender line twice or abandoning summing for the whole report. Dropping it costs nothing anybody wanted: a year of tender lines is tens of thousands of rows and is the one section that is a lookup rather than a reading. **Two sections change shape rather than staying identical.** *File info*'s bounds become the **range's**, and it names **how many batches** fall inside them — a range containing a day nobody closed is a range whose figures are short, and the batch count is the only place that shows. *Sales per hour* becomes an average **per hour of the day** across the range, capping it at twenty-four rows instead of one per hour of every day. **This is the shop's at-a-glance surface** — this month, last month, year to date — and it is deliberately the **same shape** as the close rather than a second report with its own layout, because a Manager who has learned to read the close should not have to learn a second document to read the month. *Accepted consequence, and it is a real one:* a range report is **only ever as complete as the closes inside it**, since it sums batches. **Sales in a batch nobody has closed are invisible to it** — including today's, right up until the close runs — which is why the batch count is printed rather than tucked away, and why *View Subtotal* over the open batch stays the only way to see the day you are standing in |
| 28 | **It stays a modal, and the modal scrolls. The sections collapse; File info and the Sales footer do not.** [E-05](E-05-sell-a-record.md) d28 put the close behind **Till functions** on the grounds that it is touched when nobody is waiting, and E-05 d30 gave the till a rail that overlays rather than pushes — neither changes here, and the report does not become a screen of its own. This flow's Status line stays true: **surfaced on Point of Sale, not a screen of its own.** *Rejected:* a full-screen report over the till, which reads better at eight sections and costs the one property the modal has — that the Sale underneath it is still there, unchanged, and dismissing the report puts you back at the counter mid-transaction. **Collapsing is what makes eight sections survive a modal** (decision 17's accepted consequence): sections 5 through 8 arrive collapsed, since they are the ones that answer a question rather than report the day, and the state is **not remembered** — a report is read fresh each time and an Employee should not inherit whoever last opened it. **Two things never collapse**: *File info*, because a report with its bounds folded away is the misreading decision 18 exists to prevent, and the **Sales footer**, because it is the answer to the question the report was opened for. *Accepted consequence:* the print layout and the screen layout now differ — decision 13's printed summary prints **every section expanded**, since paper has no affordance for a fold and a collapsed section on a printout is simply a missing one |

---

## Open questions

- ~~**Delivery**~~ — **Resolved** by decision 13: stored on the batch and printable, both.
- ~~**Arbitrary date ranges**~~ — **Resolved** by decision 27: the same report runs over a range, by **summing stored batch summaries**, and carries seven of its eight sections. The question's own guess was right; *Tendering details* is what summing cannot carry.
- ~~**Per-employee breakdown**~~ — **Resolved** by decision 24: it is presented, as section 7, and no longer deferred to trend reporting. (Previously: unblocked by [E-05](E-05-sell-a-record.md) d23 — a Sale belongs to whoever held the lock at tender — with the presentation left as an open reporting choice.)
- **What a range report does with an unclosed batch.** Decision 27 sums batches, so Sales sitting in an open batch are invisible to a range that covers them, and the batch count is all that says so. Whether a range ending today should *say* that today is not closed yet — or offer to include the open batch — is undecided. It is the one place the range report can be silently wrong.
- ~~**Whether section 7 is visible to an Employee**~~ — **Answered** by [architecture](../architecture.md) §11: **it is readable by any Employee, and that is the recorded position rather than an oversight.** Every read control in this system is **tenancy, not permission** — [architecture](../architecture.md) A-3 puts `store_id` and `terminal_id` in the JWT and no role, and A-38 and A-58 keep reads on the row-level-security path deliberately — so role is resolved on the **write** path and a read gate would be a **new mechanism**, not a new policy. [M-04](M-04-manage-users.md) d2 therefore governs: anything not on the manager-only list is an Employee action. *What stays live is the shop question, not the technical one:* whether a shop wants the people being compared to be able to read the comparison. Revisited if it becomes a problem rather than pre-empted.
- **Top sellers and trend reporting.** PRD goal G-2 wants market-trend analysis, which is a reporting surface well beyond a daily close. Probably its own flow.
- **Multi-store.** Whether a Manager closes per store, and whether anyone sees a consolidated position across stores. Deferred — v1 deploys one store with no cross-store UI.
- ~~**What happens to a Held Sale at close?**~~ — **Resolved** by decision 12: nothing. A Hold is untouched by the close and absent from the summary.
