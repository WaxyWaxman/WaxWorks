{
  "name": "@waxworks/tests",
  "private": true,
  "description": "Repository-level checks: the work-order rows no workspace member owns. A member so that A-102's `tsc --noEmit` and ESLint per member reach it — these files held 332 assertions, including every security assertion in the M0 review, while nothing typechecked or linted them (M0 finding 26). It declares NO `test` script: the suites run once from the root via `vitest --root .`, and a second here would run them twice.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "eslint": "^9.39.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.70.0",
    "vitest": "^5.0.1",
    "zod": "^4.6.5"
  }
}
