# Fixtures

One helper per register **Needs** entry (A-85), seeding through the actor's
surface or `supabase/seed.sql` — never a direct table write (A-4). Empty until
`/qa automate` writes the first spec.
