{
  "name": "@waxworks/db-types",
  "version": "0.0.0",
  "private": true,
  "description": "Generated from the database (architecture §7). Regenerated by the D developer in the same pull request as the migration that changed the schema; CI re-runs the generator and fails on a diff (A-98). Never edited by hand.",
  "type": "module",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "gen": "supabase gen types typescript --local --schema public,app > index.ts.tmp && node -e \"require('fs').renameSync('index.ts.tmp','index.ts')\"",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.70.0"
  }
}
