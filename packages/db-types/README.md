# @waxworks/db-types

Generated. `pnpm --filter @waxworks/db-types gen` against a running local
Supabase (`supabase start`). The D developer regenerates it in the same pull
request as any migration that changes the schema; CI regenerates and fails on a
diff (A-98). Do not edit `index.ts` by hand.
